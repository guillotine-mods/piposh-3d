# Bug tracker

A working list of known issues, separate from `docs/PLAYTEST.md` (which
tracks asset-level correctness — facing, positioning, camera angles against
the original game) and `docs/SESSION_LOG.md` (the permanent record of *how*
each fix was found and verified, written after the fact). This file is the
*queue*: what's still open, referenced by ID.

**Workflow:** say "let's work on `GB-2`" (or however you refer to it) to
start on an entry. Once a fix ships and you've confirmed it in-game, delete
that row — don't leave it marked "done," just remove it. The fix's own
history stays in `docs/SESSION_LOG.md` and the commit that made it; this
file only needs to reflect what's still outstanding.

**Adding new bugs:** append a row to the right section with the next free
number in that prefix (don't reuse numbers from deleted rows, so an ID
never means two different things across a conversation). A one-line
description is enough — add detail if you have it (which level, what you
expected vs. what happened), but don't feel obliged to.

**IDs:** `GB-*` = game-breaking, `QOL-*` = quality of life, `NB-*` =
non-breaking bugs. Prefix groups are just for scanning; nothing stops a
QOL item from being promoted to GB (or the reverse) by moving the row.

---

## Game-breaking bugs

| ID | Description | Note |
|---|---|---|
| GB-3 | Plane2: Piposh spawns lower than the plane's surface, and the character can't move. | Fixed commit `b98470e` (two separate bugs: a wrong-deck floor snap, and a camera-authority deadlock that zeroed the native controller's movement every tick). Needs re-verification in-game before closing. |
| GB-4 | Range: the upper HUD doesn't update when a citizen or terrorist is hit. | Fix shipped 2026-08-03, pending in-game confirmation. Root cause: `bmap`-declared identifiers (e.g. `bTerrHit`) evaluated to `0.0` when read as a value (only sound declarations had this fallback, not bmaps) — every `panel.bmap = X;` write across the whole corpus was a silent no-op. See `docs/SESSION_LOG.md` 2026-08-03 (GB-4). |
| GB-5 | Range: after Piposh dies, animations keep playing instead of stopping, and the retry/skip buttons that should appear don't show. | Fix shipped 2026-08-04, pending in-game confirmation. Four separate bugs: (1) the retry/map buttons on the death screen had no icon ever drawn — same gap affected every panel BUTTON in the corpus, e.g. Range's own pSkip. (2) nothing paused gameplay on death — the shared `pRIP` death-screen panel becoming visible now freezes every WDL coroutine's own progress, resuming exactly where it left off once hidden again; buttons stay clickable throughout. (3) `Death` (gates `Restart()`'s own guard) was only ever reset once, at level start, so a second death never showed RIP again — implemented `level_load()` (called by every level's own retry path) to reset it. (4) that fix's own first draft was too broad (reset ALL globals, not just `Death`) and caused a real regression, confirmed live: retrying re-triggered the intro dialogue on top of the still-running shooting view, and every terrorist got stuck unable to pop up (their own coroutines re-blocked on the same `MoviePlaying` gate) — "no hit registers, can't beat the stage" and "dying starts the dialogue again" were the same bug. Narrowed to reset only `Death`. See `docs/SESSION_LOG.md` 2026-08-03/04 (GB-5, continued, and take two). |
| GB-7 | Range: aiming/shooting works but isn't very accurate, and the mouse/aim controls feel confusing; retry doesn't reset the cursor/aim; the skip button after 3 losses isn't clickable/on top. | Fix shipped 2026-08-04, pending in-game confirmation (round 2 — user confirmed items 1/2/4 still broken and 3 partially broken after round 1; all addressed below). Round 1: (1) mouse capture for scripted-camera aiming. (2) camera reset to spawn pose on retry. (3) button-panel z-index default. (4) `_set_entity_pan()` no longer zeroing tilt/roll every tick. Round 2, after live re-test: (5) pSkip's z-index (50 vs pRIP's 20) was already correct in the built scene but still rendered underneath in real play — z-index alone isn't reliable enough for this project's Control hierarchy on its own; added an explicit post-build tree reorder (sort panels by z-index, `move_to_front()` each in order) as a second, more robust guarantee, matching how `GameHud.show_dialog()` already hedges the same way. (6) retry's camera-pose reset was deferred to `level_load()`, which sits behind `main()`'s own `wait(3)` — `action CamTarget` resumed live during that gap on the stale pre-death orientation, then snapped hard once the deferred reset landed ("retry... messes with the view"); moved the camera-pose reset to fire synchronously the instant the death screen hides instead (no gap, no snap), while `Death`'s own reset had to stay deferred — resetting it early raced against `Health` (only reset by `main()`'s later `Health=609;`) and let `action CamTarget`'s own `Restart()` call re-trigger immediately, re-showing RIP right after retrying. (7) **the real reason shots still didn't land on the crosshair even after the tilt fix**: there was no crosshair at all — `pan_cross_show()` is a real, portable WDL function (`WDL/weapons.wdl`) that positions a real panel using `screen_size`, and was already being called correctly, but `screen_size` itself was entirely unresolved (silently read as (0,0)), so the crosshair panel rendered at (-7,-7) instead of screen-center — clipped almost entirely off-screen. Combined with the OS cursor being hidden by the mouse-capture fix, the player had no on-screen aim reference at all. Fixed generically: `screen_size` now resolves to this port's fixed 640×480 panel design space (the same value used by every other `screen_size` consumer in the corpus — Golf's Booth/OnAir, Shooter's Overmap). Round 3, after live re-test again ("crosshair is better, but still hard to control with the mouse, and the location doesn't reset when restarting"): (8) the mouse stayed captured (hidden/locked) the ENTIRE time the death screen was up, so the player had no visible cursor to click Retry/Skip with at all — fixed by switching to a visible mouse while `pRIP` is shown and restoring whatever mode was active before once it hides, mirroring how `GameHud.show_dialog()` already does this for regular dialogue choices. (9) that same blind mouse-hunting was ALSO why the aim reset looked like it wasn't working: `mickey` (mouse delta) keeps accumulating every frame regardless of freeze state, so whatever motion happened clicking Retry was still sitting there the instant `action CamTarget` resumed, and got applied on top of the just-reset spawn pose before it ever reached the camera — fixed by clearing the accumulated delta in the same synchronous burst as the pose reset. Round 4 (QOL, user request rather than a new report): added a `Space` key that recenters the aim back to spawn pose at any time (not just on retry), and the OS cursor now warps to the window center whenever it becomes visible (death screen, or a `Space` recenter) instead of being left wherever it silently drifted to while captured. Round 5, after live re-test again ("still not 100% working... the skip button is still beneath the graphic that's shown when we die", "the pointer should be reset after we click to restart, not after we die"): (10) two prior pSkip-vs-pRIP z-order fixes (round 1's z-index default, round 2's `move_to_front()` tree reorder) both should have worked within `GameHud`'s single shared `CanvasLayer` and both still didn't — moved to a mechanism this project already relies on and knows works: `GameHud` now has a SECOND `CanvasLayer` (layer 21, above its own 20) that any button-bearing panel without an explicit `layer` mounts on instead, which always draws over the normal one unconditionally, the same guarantee already used for the loading screen/level-select menu. (11) moved the cursor-to-center warp from the moment `pRIP` becomes visible (death) to the moment it hides again (clicking Retry), per the request. Round 6, after live re-test again ("it spawns correctly in the middle but now 180 degrees back"): (12) `Input.warp_mouse()` (used by both the retry-click and Space recenter) can itself generate a synthetic mouse-motion event reporting the jump it just caused, arriving a frame or two AFTER the warp call — landing in the mouse-delta tracking after the existing same-frame clear had already run, and getting read as a huge real mouse movement on the very next aim tick, spinning the just-reset pose most of the way back around. Sanity-checked the spawn pose data itself first (confirmed correct — faces every Range target). Fixed with a short multi-frame suppression window armed by the warp itself. Round 7, after live re-test again ("restarting the stage after dying should reset the enemies on screen as well"): (13) an already-popped-up/hit Terrorist or Civilian stayed exactly as it was through a retry (position, tilt/roll, Pop/Dying/GoingUp, hit-reaction skin) — nothing ever put entity state back to spawn. Fixed generically: every entity now gets its exact spawn position/pan/tilt/roll restored on retry (new `wdl_spawn_position` meta). Found along the way: this port's WDL parser treats `define NAME,VALUE;` as an ordinary global variable, not a real alias — so `define Pop,skill20;` never makes `my.Pop` resolve to `my.skill20`; every named field actually lives in its own `wdl_custom_<name>` meta key. The reset clears every such key an entity has accumulated, not just the numeric skills array. Round 8, after live re-test again ("after the first reset, the character heads are not moving up and shown as they were"): (14) round 7's own fix ERASED every `wdl_custom_*` key instead of restoring a real snapshot — regressed `action Terrorist`'s own one-time `my.OriginalZ = my.z;` init (its resting height, read by both the pop-up and duck-down logic every tick, set once before the coroutine's first `wait()` and never again since the coroutine itself is never restarted on retry). Erasing it left it permanently `0.0` after the first retry, silently breaking the pop-up/duck-down thresholds for good. Fixed by capturing a REAL snapshot of every `wdl_custom_*` key right after each entity's coroutine starts (reflects its own one-time init, since coroutines run synchronously up to their first real `wait()`), and restoring FROM that snapshot on retry instead of erasing. See `docs/SESSION_LOG.md` 2026-08-04 (GB-7, GB-7 continued, GB-7 continued round 2, GB-7 continued round 3, GB-7 round 4), 2026-08-06 (GB-7 round 5, GB-7 round 6, GB-7 round 7), 2026-08-07 (GB-7 round 8). |
| GB-8 | Range: bullet-vs-target hit detection feels imprecise/unreliable ("the collision isn't working"). | **Fixed 2026-08-07**, pending in-game confirmation. Root cause found via the user's own real shot log (added 2026-08-07): every fired bullet died within a near-constant, aim-independent distance of its target (~180-193 units regardless of where the player aimed) — matching "distance from the fixed shooter position," not an actually-traveled path. Two compounding bugs: (1) `action Spark` enables impact detection as its OWN first statement, before it has moved, and bullets spawn AT the shooter's own position — the impact check's "touching" set starts empty for a brand new zone, so the shooter itself (already overlapping at that exact instant) read as "just walked in" on the very first check and killed the bullet immediately, before it ever traveled anywhere. This is generic, not Spark-specific: ANY entity spawned already overlapping another would hit the same bug. Fixed by pre-seeding the touching set with whatever's already in range at zone-creation time, silently. (2) even with that fixed, bullets kept dying almost instantly against an invisible, non-gameplay "CameraEngine" placeholder (a `Cam.MDL` marker sitting near the shooter's own line of fire, its action doesn't resolve to any real script) — flagged every marker this project already hides for being a pure logic/position helper (camera placeholders, `action Dummy`) as `wdl_non_physical` and excluded those from impact checks too. Verified: `smoke_range_aim_check.gd` now registers a real hit 3/3 runs (was 0/3 before). Round 2, after live re-test again with a fresh shot log ("the further away the terrorist is, the more likely I still can't hit him"): (3) even with both prior fixes, the impact check compares horizontal (XZ) distance only, ignoring height — correct for its original purpose (a walker's Y is artificially pinned to spawn height by a missing floor-snap, unrelated to real geometry), wrong for an actual 3D projectile: a shot aimed at a far target flies over/under any nearer target sitting in roughly the same XZ column at a different height, and since height was ignored that nearer target claimed the shot anyway — more distance simply means more nearer targets/scenery to pass by first. Fixed by using real 3D distance whenever either side of a checked pair has genuinely moved in height from its own spawn position (a bullet in flight, or a target mid pop-up/duck-down); entities that haven't moved in Y keep the original XZ-only behavior, so Shiks' own walker-vs-static-target case is untouched. Verified: `smoke_range_aim_check.gd` hit rate 10/11 across this session's runs (was 0/8 before any GB-8 fixes). Round 3, after live re-test again ("it still doesn't hit far targets accuratley"): (4) re-attempted the original "widen the hit radius to each target's own mesh footprint" fix (tried once 2026-08-06, reverted after a regression) — both root causes of that regression (spurious touch at zone creation, and the XZ-only check letting a wrong-height neighbor claim a shot) are fixed as of rounds 1-2 above, so this time it held: `smoke_range_death_freeze.gd` (the exact test that caught the original regression) now passes with it enabled. Verified hit rate 10/10 across two fresh batches, including several shots (70-90 unit closest-approach) that were misses immediately before this change. Separately noted, not yet addressed: 4 of Range's 20 targets require a tilt angle steeper than `action CamTarget`'s own `[-15,45]` clamp allows (as low as -18°) — authored in the original WDL script itself, not a port bug, but means those specific (closer, not farther) targets are physically impossible to aim at with pixel-perfect precision no matter how the mouse is moved; flagged for awareness, not fixed (would mean deviating from the original script's own clamp). Round 4, per direct user request ("can we remove the bullets and just check if the mouse clicked on the target or not?"): (5) replaced the whole physical-bullet-travel model (`ACTION Spark`'s `move()`-every-tick + this port's own approximate impact-zone check — the source of every round above) with a single instant raycast fired the moment a "Spark" would have been created, along the exact direction `CreateSpark()` already computed. Uses Godot's own real physics raycast against the real mesh collision shapes every solid entity/wall already has — pixel-precise against the actual model, immune by construction to every approximation issue rounds 1-3 above were chasing. No bullet entity is kept around at all (no coroutine, no visible presence, matching the request literally). Needed two "skip past, don't stop" exceptions, both confirmed live: entities that never called `enable_impact` (e.g. the player's own held-weapon view-model, blocking every shot at point-blank range) and pure level geometry (the shooting booth's own counter — the old `move()`-based bullet never checked wall collision at all, so skipping past it too avoids a *new* "far shots clip the counter" regression from finally respecting real geometry). Verified: `smoke_range_aim_check.gd` 10/10 across two batches, including the one target whose required tilt exceeds `CamTarget`'s own clamp (round 3's noted limitation) — the real mesh collision is forgiving enough to still register a hit there. This supersedes rounds 1-3's own fixes for Range's bullets specifically; those fixes remain in place and still matter for the *other* things that share the same generic impact-check mechanism (Terrorist/Civilian/Window/TV/Barrel proximity, and Shiks' own Bumpin/Snail trigger). Round 5, after live re-test again ("without clicking all the victims get 'hit' when they go up and i get to the 'dying' screen very quickly, can't even play") — a regression from round 4: (6) rounds 2-3's own fixes (real-3D-distance-when-airborne, mesh-footprint-widened radius) existed solely to help a *physical* bullet reliably reach a target — moot once round 4 replaced Range's shots with a raycast that never reaches this check at all, but both were left active and turned out actively harmful for the check's other real use (Terrorist/Civilian proximity to each other/scenery): Range packs its own targets only ~30 units apart, well inside the widened ~80-unit radius, and a target's own `GoingUp`/`Dying` pop-up animation is real, ongoing vertical movement — exactly what round 2's check treated as "genuinely airborne, use full 3D distance." As a target popped up, the 3D distance to its still-resting neighbor grew past the pre-seeded baseline and shrank back down on the way down, reading as a fresh "just walked in" approach each time — so a target fired its own `TargetHit` purely from popping up near another target, no shot ever fired. Reverted both rounds 2 and 3 back to the original flat-28-unit/XZ-only behavior (round 1's pre-seed fix and the `wdl_non_physical` exclusion both stay, still valid). Verified: new `smoke_range_no_self_trigger_check.gd` runs Range for 300 real frames with no shot ever fired and confirms Terrorists/Civilians counts never change (they did, spuriously, before this fix) — Health legitimately drops on its own during this test via `action Terrorist`'s own real, intentional "shoots back if left too long" mechanic, unrelated to this bug. Full Range + Shiks regression suite (19/19 dispatch, all Range smoke tests, all three Shiks impact-proximity tests) still green. See `docs/SESSION_LOG.md` 2026-08-06 (GB-7 round 6), 2026-08-07 (GB-8 continued, GB-8 fixed, GB-8 round 2, GB-8 round 3, GB-8 round 4, GB-8 round 5). |
| GB-9 | Range/Final/Smash: the first-person weapon view-model (`action Handgun`/`action Warty`, `my.near = on;`) shows way more of the forearm/sleeve than the gun itself — in the original game the gun reads as closer/cropped tighter, without most of the arm visible. | Fix shipped 2026-08-07, pending in-game confirmation. This port has no per-entity near-clip override to lean on (unlike whatever Acknex's own `NEAR` flag literally does), and the WMB-authored placement/scale were confirmed faithful to the original level file, not a conversion bug — the model itself is a full forearm-sleeve + handgun mesh (confirmed from its own texture atlas) reaching from ~34 to ~173 units in front of the camera, with the sleeve end closest (and therefore biggest/most dominant on screen) and the gun farthest (small, secondary). Fixed generically for any `near`-flagged entity: pull it toward the camera, once, along the camera-to-entity direction, by just enough that its own farthest mesh extent (the gun end) lands at a fixed close viewing distance — pushing the sleeve end behind the camera, cropped from view. Took three attempts to get the pull calculation itself right (see `docs/SESSION_LOG.md` for the full blow-by-blow): a feedback loop from deriving the pull direction off the entity's own already-adjusted live position (diverged/flipped sign), then a fixed-shift-applied-every-tick bug (compounded without bound), then a startup-ordering race against `action CamTarget`'s own first camera-position write (the pull fired before the camera had reached its real spawn pose, using a stale position). Final version anchors the direction on the entity's fixed spawn position, applies the shift exactly once (guarded), and defers that one application until at least one real frame has passed since `near` was set — guaranteeing every other entity's coroutine (including the camera-driving one) has had a chance to run first. Verified via a headless transform/mesh-AABB dump: the gun end now lands at ~40 units in front of camera (the chosen target distance) and the sleeve end at ~99 units *behind* the camera. Full Range + Shiks regression suite still green. Round 2, user request after confirming the close-up pull worked ("make the gun a bit lower on the screen"): added a second, vertical one-time offset (`NEAR_WEAPON_VERTICAL_DROP`) applied along the camera's own up vector at the moment of the (still one-time) pull, dropping the model toward the bottom of frame instead of sitting at eye-level/screen-center — same timing/guard machinery as the depth pull, just a second offset added to the same result. Verified: camera-local AABB Y range moved from straddling center (-25 to +8) to fully below it (-45 to -12). See also GB-10 for a second, unrelated bug found investigating the same user report ("the mouse should always be centered like the aim cursor"). Round 3, after live re-test again ("the only thing not resetting correctly is the view of the gun/hand, that now we see the gun and the hand in front of us instead of first person POV like before the retry") — turned out to be what round 1/2's "mouse cursor" report actually meant, not a separate mouse-mode bug (see GB-10's own note; that fix stands regardless): `_reset_all_entities_to_spawn()`'s per-entity reset (`_reset_entity_to_spawn()`) puts EVERY entity's position back at its raw WMB spawn point on retry, including the Handgun — correct for every other entity, but the Handgun's raw spawn point is deliberately wrong, only ever meant to be a starting point for the near-pull. That pull is guarded by `wdl_near_applied` so it can't reapply itself every tick, but nothing ever cleared the guard on retry either, so the reset undid the pull and the guard then permanently blocked redoing it — the weapon view stuck at its far/full-body pre-GB-9 framing for the rest of that playthrough. Fixed by clearing `wdl_near_applied`/`wdl_near_activated_frame` in `_reset_entity_to_spawn()` whenever the entity carries `wdl_near`, re-arming the pull exactly like a fresh level load; `action Handgun`'s own still-running `while(1)` loop keeps writing `my.pan`/`my.roll` every tick regardless of retry (its coroutine is never restarted, only unfrozen), so the pull reapplies on the very next tick. Verified via new `smoke_range_retry_near_weapon_check.gd`: the Handgun ends up at the exact same pulled-in distance from camera before and after retry (97.55 units both times, vs. the raw unpulled spawn distance of 38.05). Full Range + Shiks regression suite still green. |
| GB-10 | Range (and any level that shows a dialogue before mouse-look gameplay): after the intro dialogue closes, a real OS mouse cursor stays visible and freely movable for the rest of the level, separate from and confusing next to the drawn crosshair. | Fix shipped 2026-08-07, pending in-game confirmation. Root cause: `GameHud.show_dialog()` unconditionally forces `Input.mouse_mode = MOUSE_MODE_VISIBLE` (needed for real OS-cursor hit-testing on the dialogue's TextureButtons), but `hide_dialog()` never put it back — Range's own `main()` (`original/piposh3d/Range.wdl:288-311`) shows a real dialogue choice before `action CamTarget`'s mickey-based aiming ever starts, so the mouse mode that dialogue forced stuck around for the entire shooting-gallery phase afterward, since nothing else in the per-level startup path re-asserts it (the earlier `Input.mouse_mode = CAPTURED` write in `level_runner.gd`, gated on `WdlInterpreter.uses_mickey_aiming()`, runs once at level setup, well before the dialogue even shows, so it's silently overwritten later). Fixed the same way `WdlInterpreter`'s own `_mouse_mode_before_rip` already saves/restores mouse mode around the pRIP death panel: `show_dialog()` now saves `Input.mouse_mode` before forcing it visible, and `hide_dialog()` restores it (plus `mouse_look`/the custom cursor sprite's visibility, kept consistent with the restored mode). Verified: full Range regression suite plus Shiks' own dialogue-choice tests (`smoke_shiks_dialog2`, `smoke_shiks_dialog2_choice`, `smoke_shiks_dialog2_choice3`, `smoke_shiks_dialog_choice`) all still pass. Left the `[mouse-mode]` debug logging added while chasing GB-11 in place (harmless, `PiposhDebug.ENABLED` gated) since it turned out to describe the same GB-9 round 3 issue, not a real mouse-mode bug — see GB-9's own note. |
| GB-12 | Plane3 (the level after Range): loads and shows the scene, but nothing ever progresses — no camera cuts, no dialogue, no character animation, permanently frozen on the very first line. | Fix shipped 2026-08-08, pending in-game confirmation. Root cause: Plane3's own `action Dome` (`BackDome.MDL`, WED-authored scale 95.36 — a background sky-dome, common in this corpus for slowly-rotating backdrops) is the ONLY entity in the level that polls `GetPosition(Voice)` and advances the `Scene` global (`if (Scene > -1) { if (GetPosition(Voice) >= 1000000) { Scene = Scene + 1; SetVoice(); } }`) — and every camera cut (`action Cam`'s own `Stage`/`CamShow` gates), dialogue line, and character animation state in the whole level gates on `Scene` in turn. `WmbLevelLoader._spawn_entity()`'s own `MAX_UNIFORM_SCALE` cap (64.0, meant to reject decorative skybox junk too large to be real geometry) silently rejected this entity before it ever spawned — no node, no coroutine, nothing to ever advance `Scene`, so the level sat frozen on its first line forever, matching the report exactly ("level loads but nothing moves/progresses"). Confirmed via a from-scratch headless trace: `Wait.wav` (Scene 0's own filler sound, ~8ms long) already reports "finished" from the very first frame checked, but `Scene` still never moved past `0.0` over 300 simulated frames — the poller itself (Dome's coroutine) simply didn't exist. A corpus-wide survey of every level's own entity scales found only one other placement above the old 64.0 cap — Mount's own BackDome at 197.82, `action=null` (purely decorative, no script depends on it existing) — so raising the cap to 100.0 picks up Plane3's real, functionally-required dome without changing Mount's (already-excluded, unused) one at all. Verified via new `smoke_plane3_dome_scale_check.gd`: the Dome entity now spawns and `Scene` climbs 0→1→2→3 over real simulated voice-line completions instead of sitting stuck. Full `smoke_dispatch --all` (55/56, unchanged) plus a Range/Shiks spot-check still green — this touches the shared level loader, not a Plane3-only code path. |
| GB-13 | Range: the Skip button (appears on the death screen after 3+ deaths) plays a sound but never actually leaves the level. | Fix shipped 2026-08-08, pending in-game confirmation. Confirmed live: clicking Skip DOES register (its own `sPlay("SFX138.WAV")` audibly fires) but never gets any further — meaning `function D1 { sPlay(...); while (GetPosition(Voice) < 1000000) { wait(1); } Run("Plane3.exe"); }` was deadlocking inside its own wait loop. Root cause: GB-5's own `_frozen` flag (added to pause world-entity coroutines — Terrorist/Civilian/CamTarget/Handgun — while the pRIP death screen is up) gates EVERY `wait()` statement in the interpreter, with no distinction for WHICH coroutine is waiting. Retry's own `fRIP1 { HideRIP(); main(); }` never actually hits this gate in practice, since `HideRIP()` clears `_frozen` synchronously as part of the SAME statement, before `main()`'s first wait is ever reached — but Skip is deliberately clickable WHILE pRIP stays up (an alternative to Retry, not something requiring the death screen to close first), so nothing ever unfreezes anything, and D1's own `wait(1)` blocked forever. `fRIP2` (pRIP's "return to map" button) has the identical shape (`waitt(60); ...; wait(1);`) and would have deadlocked the same way. Fixed by exempting bare (`my == null`) coroutines — every panel-button-invoked function in the corpus, never a world entity — from the freeze gate, mirroring how `main()`'s own `my == null` first wait already gets special-cased for the same underlying reason (UI/level-lifecycle-driven coroutines aren't part of the "world" `_frozen` is meant to pause). Verified via new `smoke_range_skip_button_check.gd`, which reproduces the exact reported scenario (click Skip while `_frozen` is still true, without clicking Retry first) — now correctly reaches `Plane3`. Full Range regression suite (18 tests) plus `smoke_dispatch`/Shiks all still green — this changes the interpreter's own core `wait()` handling, used by every level. |
| GB-14 | Plane3: the dialogue-choice text box shows only "…" for every line, and the camera visibly moves/drifts even though the level is a fully scripted, non-interactive cutscene. | Fix shipped 2026-08-08, pending in-game confirmation. User explicitly asked for engine-wide fixes here, not per-level patches — both root causes turned out to already have that shape available. **Dialogue text**: `GameHud._dialog_lines()` was a hand-maintained GDScript table, hardcoded per `DialogIndex`, that only ever had 5 of the corpus's 57 real dialogue indices transcribed (0-4, covering Plane/Range's own dialogues) — every OTHER index, including Plane3's own (5, 6), fell through to a literal `["…","…","…"]` placeholder. Found that `original/piposh3d/WDL/DIalog.wdl` (windows-1255 encoded) already carries the REAL, already-decoded Hebrew text for every one of its 57 `DialogIndex` blocks as trailing `//` comments on each `txtN.string = "<cipher placeholder>";// <real text>` line — the original developer's own reference copy, never previously read by this port. Added `tools/extract_dialog_text.py` (a new, generic asset-pipeline step matching this project's existing WMB/WDL/SFX extraction scripts) that pulls all 57 blocks into `assets/converted/dialog_text.json`, and rewrote `_dialog_lines()` to load that JSON at runtime instead of the hardcoded table — covers every level's dialogue at once, not just Plane3's, and needs no further hand-transcription ever again. Verified byte-identical against the 5 previously-hand-transcribed indices. **Camera drift**: `WdlDirector`'s own `_unhandled_input()` had a generic "mouse-look while scripted camera" feature that directly mutated whichever Cam entity was currently active's own `pan`/`tilt` meta on every `InputEventMouseMotion`, gated by a hand-grown exclusion list (Start/Shiks/Plane/Range/Studio — Studio added 2026-07-31 after an identical "mouse moves the camera when it shouldn't" report) that nobody had added Plane3 to yet. A corpus-wide grep (`grep -l mickey original/piposh3d/*.wdl`) confirmed every level that actually wants free mouse-look (Town, Golf, Range) already implements it completely within its own WDL script, executed correctly by the interpreter — this generic fallback was always either fully redundant (those three) or actively wrong (everyone else, an opt-out list can only ever catch levels after someone notices and reports them). Deleted the whole feature (and the three now-unused `_is_shiks_level()`/`_is_plane_level()`/`_is_studio_level()` helpers) instead of adding a sixth exclusion. Verified via new `smoke_dialog_text_check.gd` and `smoke_plane3_camera_static_check.gd` (a real `InputEventMouseMotion` burst no longer moves the camera at all), plus Studio's own two existing dialogue/camera tests confirming no regression there. Also: F6 now jumps to `Plane3` instead of `Range` (was hardcoded to whichever level was "next" during the previous session; updated per direct request now that Plane3 is the active level being worked on). Two more reports from the same message — "graphics and animations not running correctly" and "the backgrounds in the 'outside world' don't look correct" — are still open; nothing conclusive found from code/log inspection alone (headless can't render for a visual check), need more specifics or a screenshot to chase further. |
| GB-15 | Plane3: after the 2nd real dialogue choice, the "bird catches the vase" cutscene (`action BadBird`) plays a wrong animation on impact, Piposh's own movement animation looks like a plain walk cycle instead of the correct one, and — the severe part — **the game gets permanently stuck** ("mid flight where the vase is"), blocking `Run("Smash.exe")` entirely. | Stuck-game part fixed 2026-08-08, pending in-game confirmation; the two animation-quality reports are still open (see below). Three separate, real interpreter bugs stacked together to cause the hang, found by tracing the exact `remove()` call site rather than guessing: (1) `GetPosition(Voice)`'s debounce (added for a prior bug) keyed only on `(caller, generation)`, so a SINGLE entity with two independent poll call-sites within its own coroutine — Dome's Scene-advance poll and its own later dialogue-wait loop — starved each other; fixed by stamping each call-site AST node with a stable id (`_call_site_id()`) and keying the debounce per `(caller, site, generation)`. (2) `_do_create()`'s 3rd (initial-action) argument only ever checked `_actions`, never `_functions` — so `create(<gibbit.mdl>, MY.POS, _gib_action)` (WDL/war.wdl's `_gib()`, a real shared-library helper, not an engine builtin — spawns explosion debris, called by BadBird right before the vase catch) spawned 20 entities tagged with an action name but never started their coroutine; fixed with a `_resolve_function()` fallback. (3) The actual game-breaker: `my = X;` — WDL's reassignable `MY`/`ME` pointer register — was a silent no-op in this interpreter. `_get_var("my")` has always correctly returned the real per-coroutine `my` parameter, but `_set_var("my", ...)` (reached via plain assignment) had no matching special case and just wrote into a dead, never-read global. `action BadBird`'s own script does exactly `my = TheVase; _gib(20); actor_explode(); my = Birdy;` — a completely standard Acknex idiom for "run this generic library action ON a different entity". With the reassignment silently doing nothing, `actor_explode()` (`WDL/weapons.wdl`, a real shared `ACTION` that ends with `remove(ME);` after a few real `wait()`s) ran against BadBird itself instead of TheVase — removing BadBird mid-script, which (via `_entity_alive()`'s guard at the top of every statement) silently halts all of its own further execution, including the `while(GetPosition(Voice)<1000000){...}` / `Run("Smash.exe")` tail that was the level's only exit. Confirmed via a targeted trace at the `remove()` call site itself (not the GDScript call stack, which turned out to be a red herring — Godot's own coroutine-resumption chaining makes `get_stack()` misleading across multiple interleaved `await`ed WDL coroutines): `raw_arg0={"t":"id","name":"ME"}, my=BadBird`, and `WDL/weapons.wdl:2000`'s `ACTION actor_explode` body ends in exactly that `remove(ME);`. Fixed generically, not as a Plane3 patch: `exec_block()`/`_exec_block_sync()` (the two statement-list walkers used by every action/function/if/while body in the interpreter) now track a rebindable `current_my`, intercepting a bare top-level `my = X;`/`me = X;` statement and threading the new value through every sibling statement that follows in the same block (and everything they call) instead of letting `_set_var()` silently drop it. Scoped to the block: a reassignment inside a nested `if`/`while` doesn't propagate back out to the enclosing block, an accepted gap since every real corpus usage found (including BadBird's own) reassigns and consumes `my` as direct siblings in one block. Verified via new `smoke_plane3_vase_catch_check.gd` (BadBird survives the catch, level transitions to `Smash`) plus the full targeted regression set: `smoke_dispatch`, `smoke_remove_race`, `smoke_plane3_dome_scale_check`, `smoke_plane3_camera_static_check`, `smoke_dialog_text_check`, `smoke_range_skip_button_check`, `smoke_plane2_all_goals`, `smoke_plane2_playtest`, three Range tests (`retry`/`hitscan`/`death_freeze`), and Shiks' three dialogue-choice tests — all green except `smoke_shiks_dialog2_choice.gd`, confirmed (via `git stash` back to the pre-session baseline) to already fail identically before any of today's changes, not a regression. The two animation-quality reports from the same message (wrong impact animation, Piposh's own movement looking like a walk cycle) are still open. Follow-up (2026-08-08), now fixed: root-caused the "looks like walking" report to `action PipFall`'s own `ent_frame("Fetch", my.skill1)` (the correct reach pose) and `_do_actor_move()`'s generic `wdl_auto_walk_anim` fallback (a hardcoded "Walk" cycle, added 2026-08-01 for Plane's own `PiposhWalk`) both firing in the same tick while flying toward the vase, with the fallback running second and stomping "Fetch" every time. First attempt (suppress the fallback whenever ANY real `ent_frame`/`ent_cycle` call already fired that same tick) regressed `smoke_plane_walk_anim.gd`: Plane's own `Blink()` helper unconditionally calls `ent_frame("Stand",0)` every tick in the identical "real anim call, then actor_move(), same tick" shape, but there the walk-cycle fallback winning is correct, so a plain same-tick-ordering rule couldn't tell the two cases apart. Found the actual distinguishing signal via a corpus-wide `grep -B2 ent_frame`: "Stand"/"stand" is used as an `ent_frame` target 105 times across the corpus, by far the most common name, and it's exactly what every animating `Blink()`/`Blink2()` variant uses as its own idle/default reset pose — semantically never a pose a script would deliberately want to hold while `actor_move()` is actively translating the entity. Fixed by excluding "Stand" specifically from the "a real pose already won this tick" marker (`_do_anim_frame()`/`_do_anim_cycle()` skip the stamp for that one clip name): Blink's "Stand" is now correctly treated as the throwaway default it is (walk fallback still wins), while PipFall's "Fetch" (or any other named pose, corpus-wide) correctly suppresses the fallback. Verified via new `smoke_pipfall_fetch_anim_check.gd` (`saw_fetch=true saw_walk=false` across the real, un-forced flight window) plus re-confirming `smoke_plane_walk_anim.gd`/`smoke_plane_walk_height.gd` still pass, plus the full GB-15 regression set. Not a Plane3-only fix — applies to every one of the 24 corpus-wide actions that combine `actor_move()` with `ent_frame`/`ent_cycle`. Follow-up (2026-08-09), now fixed: the impact-animation report turned out to be about `_gib(20)`'s own 20 debris entities not actually flying apart — they spawned and animated correctly (GB-15's earlier `_do_create()` fix) but sat frozen at the vase's exact position instead of scattering outward, so the "explosion" read as "the vase just vanishes." Root cause: `WDL/war.wdl`'s own `_gib_action()` computes its per-tick velocity into a plain array global (`abspeed[0]=...`, declared `var abspeed[3];` in `WDL/movement.wdl`) and applies it via `MOVE ME,NULLSKILL,abspeed;` — but `_vec_get()` (the reader every vector-consuming builtin, including `MOVE`, goes through) only ever handled Acknex's OTHER vector spelling (a scratch var's own `.x`/`.y`/`.z` fields, e.g. `temp.x = ...`), so a bare array-style vector like `abspeed` silently fell through to `Vector3.ZERO` regardless of what the script had written into it. Fixed by having `_vec_get()` also check for a plain Array-valued global via `_get_var()` before giving up. Not gib-specific or Plane3-specific: the same `abspeed`/`MY_SPEED` array-vector idiom appears in `Desert.wdl`'s own debris action and `WDL/auftrag.wdl`. Verified via new `smoke_gib_debris_movement_check.gd`: all 20 spawned debris entities now move a real distance from their shared spawn point (confirmed via `git stash` that the pre-fix baseline showed 0/20 moving, matching the reported symptom exactly). Also newly reported: Smash (and possibly levels after it) showing similar "character not moving/facing right" and "character visible before it should be" symptoms — checked whether this is a parsing/dispatch gap (per the user's own question) via `smoke_dispatch.gd --all`: it isn't, Smash reaches the interpreter exactly like every other level (`has_ast=true interpreted=true`), so these are specific interpreter-behavior bugs (likely in the same shared code as the issues above, e.g. `wdl_auto_walk_anim`, or the existing `_seed_reveal_only_hidden()` visibility-scan not catching every authoring shape) rather than a systemic parsing failure. Not yet chased further — needs either specific repro details (which character/entity, which moment) or a screenshot, since headless can't render for a visual check. |

| GB-16 | Smash (and possibly later levels): "character not moving/facing right" and "character shown before it should be" — no specific entity/moment named yet. | Open, audited 2026-08-08. Confirmed via `smoke_dispatch.gd --all` this isn't a parsing/dispatch gap (see GB-15's own note). Read all ~25 actions in `Smash.wdl` end to end looking for known bug shapes: `GetPosition(Voice)` debounce collisions (`action PipTalk` has ~8 independent call sites in one coroutine — already covered by GB-15's generic per-call-site fix, not a new issue), the actor_move()+ent_frame stomp (none of Smash's own actions combine them the way `PipFall` does), and reveal-only visibility gaps (`_seed_reveal_only_hidden()`'s own scan). Directly verified via a headless trace that the obvious "reveal-only" candidates (`BigBad`/`Bads`/`PipRide`/`PipPee`/`PipSit`/`Mendy`/`Ami`, all WED-authored *visible* but toggled invisible by their own script's first tick) are already correctly hidden from frame 1 onward — no premature-visibility flash for these specifically. The one real, structural finding: `action Dance` (Smash's party-crowd/dance-floor entities — Cheer/Dance/Jackson/Headspin/Cool poses, face-camera-and-talk, sit-until-the-music-starts) branches its ENTIRE behavior on `my.flag1`/`flag2`/`flag3`/`flag5` — and this port has no verified WED bit mapping for the `flag1`-`flag8` custom checkboxes (only bit0=INVISIBLE and bit10=PASSABLE are confirmed; see `docs/CONTRACT.md` §"known gaps"). Confirmed via the raw level JSON that Smash's own 11 `Dance` placements carry genuinely distinct flag values (mostly bit0-only i.e. correctly invisible/unused placements, but 3 with bits 1/2/4 set and no bit0 — i.e. WED-authored visible, and their entire pose/behavior depends on bits this port can't currently decode). This is a real, plausible source of "not facing/moving right" for the party scene specifically, but per `docs/CONTRACT.md`'s own explicit standing guidance, do **not** guess a general flag1-8 bit mapping without human-confirmed measurement — a wrong guess would apply corpus-wide to every level's own independent use of flag1-8, not just this one (the one prior exception, `action LookAtMe`'s `flag1`, was scoped narrowly and required direct user sign-off after a documented bit-mapping dead end, not a blind guess). Needs either specific repro details/a screenshot to identify which OTHER concrete symptom the user meant, or explicit sign-off to attempt a narrowly-scoped, `Dance`-only flag1/2/3/5 guess-and-verify (same shape as the `LookAtMe` precedent) before any code changes here. |

| GB-17 | Smash playtest (2026-08-09): (1) "standing Piposh... appears both falling and on the floor when the game starts"; (2) "The Genia model is walking to the correct direction but facing the wrong way, he needs to be facing 90 degrees to his left"; (3) "The backgrounds (drawings on the horizon itself) don't look correct at all, it looks like a weird cloud pattern with black background"; plus two general asks: "a very high volume of cars honking that shouldn't be so loud (lets have an FX / voice volume control in settings?)" and "no animation of the pee when piposh pees." | All fixed 2026-08-09. (1) `action PipTalk` (WED-authored visible) only reveals when `MoviePhase==1\|\|3` and only hides when `Ride>0` — two independent, non-exhaustive conditions both false at level start, so `action PiposhFall`'s own visible-and-falling entity had a duplicate visible-and-standing PipTalk sitting on the ground the whole time. Generalized `_seed_reveal_only_hidden()`'s own detection (new `_has_exhaustive_invisible_toggle()` / `_has_unconditional_leading_invisible_set()` helpers) to recognize this "staged reveal with a gap" shape corpus-wide, not just the original "never hides at all" one it already handled — spot-checked a sample of the ~13 other corpus actions this newly affects (Final's own Bullet, Shooter's Plane1/2, InShrine's Miner) and each turned out to be a genuine additional fix, not a risk, confirmed via direct source reading. (2) `action WalkGeniaWalk` moves via direct position writes and never touches `my.pan`; WED authored `angle_gs=[0,0,0]`, which (per `_do_actor_move()`'s own pan convention) faces +Godot X, not Genia's own +Godot Z direction of travel — fixed via a targeted `_seed_genia_facing()` spawn-time correction (`pan -= 90`), scoped to this one action like the existing `LookAtMe`/`PIPI` precedents. (3) Horizon strip textures (`horizon1.png` etc.) are drawn as a building silhouette over a genuinely transparent sky region (confirmed via direct pixel inspection: ~17-27% of every horizon texture in the corpus is alpha=0 with RGB=(0,0,0)) — `acknex_sky.gd`'s own scene-map cylinder material never enabled alpha, so those "transparent" pixels rendered as solid opaque black, and the real sky dome (a mostly-white `sky.png` blended with `clds.png`'s own dark cloud silhouettes) only showed through wherever the cylinder didn't cover at all — read as "a weird cloud pattern with black background" instead of a clean skyline. Fixed by setting `TRANSPARENCY_ALPHA_SCISSOR` on the material (a hard cutout, not smooth alpha blending, since every alpha value found was binary — avoids transparency-sorting artifacts on a large cylinder). Applies to every level using a scene_map horizon strip, not just Smash. (4) Added real Voice/SFX/Music volume control: three `AudioServer` buses (`autoload/audio_channels.gd`), each channel's player(s) routed to its own bus, persisted to `user://audio_settings.cfg`, and a new `SettingsPanel` (`scripts/ui/settings_panel.gd`, one slider per channel) toggled via F9 from both the level scene and the main menu — also closes part of the long-standing QOL-6 backlog item. Separately trimmed Smash's own Honk1/2/3 (SFX007/008/009.wav) by -9dB in the existing `SFX_VOLUME_TRIM_DB` table (matching the established sHammer/Jet precedent) for the specific reported loudness, which is from many overlapping traffic cars stacking full-volume honks, not a mislabeled-range issue like those two. (5) Acknex's own particle builtin, `emit(quantity, position, function)`, was entirely unbridged (used by `action PipPee`'s own `emit 2,temp.x,stream;`, and also Smash's Vespa exhaust and PieceFall dust) — genuinely interpreting the named particle function's own body would need a whole separate execution context for its `MY_AGE`/`MY_SPEED`/`MY_MAP`/etc pseudo-fields, a substantially larger undertaking than the report warranted. Implemented a scoped-down but real version instead: `emit()` now spawns physically-reasonable fading/scattering sprite particles (outward-and-up scatter, gravity, fade-to-nothing) at the given position — not a byte-faithful simulation of the named function, but a real, visible effect where there was none before, verified to correctly grow while emitting and fully clean up (zero leaked nodes) once emission stops. Verified via three new tests: `smoke_smash_visuals_check.gd`, `smoke_emit_particles_check.gd`, `smoke_audio_volume_settings_check.gd`, plus the full targeted regression set (`smoke_dispatch --all`, Range/Plane2/Plane3/Plane/Shiks spot-checks) all green. |

| GB-18 | Follow-up playtest report (2026-08-09) on GB-17's own fixes: Genia's own walk animation still not playing (only fixed his facing, not the animation itself), the new SFX volume slider not responding to input at all, some world backgrounds still wrong, and low-quality/missing-geometry models (e.g. "the plane in the takeoff animation doesn't have wings") — asked directly whether this could be a systemic high-res/low-res asset selection issue. | Three of four fixed 2026-08-09; the model-quality report is still open. **SFX slider unusable**: FP levels (Range, Plane2, ...) keep `Input.mouse_mode` CAPTURED (cursor hidden, locked to screen center) the whole time they're playing — opening `SettingsPanel` over that state left the slider impossible to click or drag. Fixed by saving/restoring mouse mode around the panel's own open/close, same pattern as `GameHud.show_dialog()`/`hide_dialog()`. **Genia's animation**: root cause was NOT the facing fix at all — `action WalkGeniaWalk` calls `Blink()` (→ `ent_frame("Stand",0)` → `play_frame()`, which unconditionally resets `_current_clip`/`_playing`) every tick, immediately before its own `ent_cycle("Walk", my.skill1)` — defeating `MdlAnimator.play_cycle()`'s own "already playing this clip, let `_process()`'s natural per-frame advance continue" guard on every single tick. `my.skill1` is a plain, ever-growing accumulator (the standard corpus idiom, no wrap-around of its own), so `clampf(percent,0,100)` froze `_percent` at exactly 100.0 within well under a second of walking — the exact same clamp-vs-wrap bug already found and fixed once before (2026-08-02, Plane's own PiposhWalk), but that fix lived inside `_do_actor_move()`'s own auto-walk fallback, never covering a WDL script calling `ent_cycle()` directly. Fixed at the real, shared source this time: `play_cycle()` now wraps via `fposmod` instead of clamping — covers every corpus action with this same "another `ent_frame` call interleaved every tick" shape, not just Genia. **Some backgrounds still wrong**: found a second, real bug in `assets/converted/wdl_meta.json`'s own static `scene_map` extraction — it picks whichever `scene_map = X;` assignment appears textually LAST in a level's own source, correct for Smash's single unconditional assignment but wrong for Desert.wdl, which picks one of six horizon textures based on `Stage` (read from a save file at level start). Moved `level_runner.gd`'s own `_apply_wdl_sky()` to run after `_director.setup()` and added a bounded per-frame poll for the interpreter's own live `scene_map` value, overriding the static guess once it's actually known — correctly fixes any level where the real value becomes available (confirmed: Smash unaffected, unconditional case still works identically). Discovered along the way, and fixed as a real prerequisite: `file_open_write`/`file_open_read`/`file_asc_write`/`file_asc_read`/`file_close` were entirely unbridged (zero matches anywhere in the interpreter) — Desert's own `Stage` read from "Arrive.dat" (written by Map.wdl's own `LocationGo()`) silently stayed at its declared default without them. Added a real, scoped-down bridge (plain-number values only, not the by-reference string variant, and not `WDL/IO.wdl`'s own much larger sequential save/load-game system through the same handles — a separate, bigger undertaking left untouched) under `user://`. **Follow-up (2026-08-09), now also fixed**: Desert.wdl's own `main()` calls a weather helper (`let_it_rain()`/`storm()`, depending on `Stage`) BEFORE its own `scene_map=...` lines whenever the destination isn't the default — that helper calls its own `_akt()` companion (`rain_akt()`), a genuine `while(weather==...){...wait();}` forever-loop meant to run as an independent background task for the rest of the level. Called as a bare, non-tail statement, `_call_user_function_async()`'s own "await the whole callee" semantics were blocking `main()` at that exact statement forever (the callee never naturally returns), so `scene_map` — along with everything else later in `main()` — never got set at all for Village/Asylum/Olympic/Volcano destinations specifically (confirmed live via a targeted trace: `WeatherSet` also never left its own declared default). Traced through the two callers this fix's own risk assessment worried about, live, before touching anything: `perform_handle()` turns out to never actually reach `_call_user_function_async()` at all — it's listed in `BRIDGE_OVER_SHARED_FUNCTIONS` and gets intentionally intercepted earlier as a no-op (this port's own native player controller handles input directly), confirmed via a temporary trace showing zero calls into the function for the entire duration of a real Plane2 run; and `player_move2()` (an `ACTION`, going through the parallel action-invoked-as-function path, not this one) is called in genuine tail position — `action player_walk2`'s own literal last statement — where "block forever" and "detach, caller has nothing left to do anyway" are observably identical. With neither actually at risk, fixed both paths the same way: for a zero-parameter callee specifically (the param save/restore logic isn't safe to detach otherwise — it assumes restoring the instant the call "returns" is safe, true when there's nothing to restore), call the body without `await` — the same `_run_coroutine()`-style fire-and-forget already used to start every entity's own initial action, which GDScript still runs synchronously up to its own first real suspension without blocking the caller past it. (Discovered along the way: GDScript's own compiler statically refuses to let a coroutine's return value be captured without `await` at the call site at all — the fix calls it as a bare statement, discarding the return, not merely omitting `await` on an assignment.) Verified: `WeatherSet` now correctly reaches 1.0 and Desert's own scene_map now correctly resolves to `Horizon2.png` for a `_VILLAGE` destination (was permanently stuck on the static `horizon6.png` guess before), and `smoke_plane2_all_goals.gd` (the existing regression test for the two callers this touches) still passes unchanged. **Model quality (plane wings)**: still open, inconclusive without more information. Checked the user's own "high-res/low-res mode" hypothesis directly: the asset-conversion pipeline (`tools/convert_mdl.py` and friends) has no LOD/quality-tier selection logic at all (confirmed via grep), so it can't be picking a "low-res" variant by mistake — whatever model a level references is what gets converted, unchanged. Checked whether the plane model is assembled from separate fuselage+wing pieces that might be spawning independently (a real corpus pattern elsewhere, e.g. a standalone `Wing1.MDL` exists) — Plane2's own placements (the likely "takeoff" level, `BiPlane2.MDL`, `action Land`/`item_pickup`) use a single combined model, not split pieces, so a missing-companion-entity theory doesn't apply either. Couldn't go further without a real visual check (headless Godot can't render a frame, a known, longstanding limitation) or a specific level/moment named — needs a screenshot or more detail to make concrete progress. |

| GB-19 | Follow-up (2026-08-09): "textures look wrong/low-res" (a broader concern beyond the specific plane report) and "the plane in the takeoff animation doesn't have wings" (Plane2, the `action B747` takeoff sequence specifically). | Textures fixed 2026-08-09; the plane/wings report was re-opened and re-investigated in GB-20 below with an actual screenshot. **Textures**: found a real, systemic split — every texture under `assets/converted/gfx/` (2D UI/panels/backgrounds, 535 files) already uses lossless `compress/mode=0`, but every texture under `assets/converted/mdl/` (3D model/character skins, 648 files) used lossy VRAM compression (`compress/mode=2`, Godot's own S3TC/ETC2 default for textures it detects as 3D-material-attached). Given how small these source textures actually are (most 320×200 or smaller, several non-power-of-2), block-based VRAM compression's own artifacts are disproportionately visible relative to the image size — exactly what "low-res" reads as, and the exact reasoning the `gfx/` folder was apparently already built around. Bulk-changed all 648 `.import` files to `compress/mode=0` and re-imported (Godot correctly regenerated the derived `[remap]`/`metadata` sections to match, `mipmaps/generate` correctly left untouched since that's unrelated to compression mode) — verified via a full A/B regression comparison (`git stash` back to the lossy baseline and forward again) that this introduces no functional regression, only a quality change. **Plane wings (first pass)**: user confirmed the specific location as Plane2's own takeoff cutscene. Traced the real model past two wrong turns — `BiPlane`/`BiPlane2.MDL` (small ground-decoration planes, action `Land`/`item_pickup`, initially assumed based on the name alone) turned out to be the wrong model entirely; the real takeoff sequence uses `action B747` (`B747.MDL`, a Boeing 747), found via `action CamPlane`'s own camera-follow logic referencing it. Verified the model itself directly, bypassing Godot's own headless-rendering limitation: extracted `B747.glb`'s own vertex/triangle data straight from its binary buffers and rendered a real top-down wireframe projection — a complete, correctly-proportioned 747 silhouette, wings, all four engine pods, and tail all present and intact (4358 triangles). See GB-20 for the follow-up investigation once a real screenshot was available. |
| GB-20 | Playtest report (2026-08-10), with a screenshot from Plane2's takeoff cutscene: "the wings appear after the plane starts to take off but not before"; "the pee animation is still not working right"; "the first [t]own isn't showing correctly... no running cars, some of the 'land' is missing"; "the background has the same weird cloud pattern in all of the area... in all of the game's levels." | Three of four fixed 2026-08-10; the plane-wings report re-investigated in full and not reproducible as a bug (see below). **Background cloud pattern (all levels)**: root cause was never the horizon cylinder GB-17 already fixed — it was the sky DOME itself. `sky.png` (256×256) is 99.9% fully-transparent pixels (a handful of white star dots), and `clds.png` (320×320) is ~85% opaque near-black pixels; `AcknexSky._make_sky_panorama()` wrote these straight into the panorama image, alpha included, but `PanoramaSkyMaterial` reads a panorama's RGB directly and has no notion of alpha at all (nothing "behind" a sky dome to blend against) — so nearly the whole dome rendered as solid opaque BLACK, with the sparse star/cloud fragments reading as "a weird cloud pattern" on it. Fixed by compositing both layers onto a real opaque blue-sky gradient base first, using each layer's own alpha as blend weight. Shared by every level via `wdl_meta.json`'s own defaults, so this is a genuine one-fix-for-all-levels change, matching the report. Verified via new `smoke_sky_panorama_check.gd` (0% black pixels, was ~90%+). **Town: no running cars**: traced through three independent, stacked bugs. (1) `action SportCar` (the runtime-`create()`d traffic car) never set `_MOVEMODE` to a positive value anywhere in its own script, unlike every other `scan_path()` caller in the corpus (Start/Fight/Race/AsyAct1/...), which all explicitly set `my._movemode=1;` first — SportCar instead relies on real Acknex's own `scan_path()` setting `_MOVEMODE` on success as a side effect, which this port's `_do_scan_path()` never replicated; fixed by setting it there too, a strict superset of the existing explicit-set convention. (2) The generic `move(ENT,dist,absdist)` bridge only ever applied `absdist` (world-space, for gravity/jump), silently dropping `dist` (entity-local, for ordinary forward walking/driving) — harmless for the corpus's other `move()` idiom (stationary projectiles) but breaks every ground actor using the real `dist,absdist` pattern (Town/Fight/Mount/Mine/Race/WDL/Cards.wdl/WDL/PWF.wdl); fixed by rotating `dist` into world space via the entity's own pan/tilt/roll and applying both vectors. (3) `force = my.skill1;` (a bare scalar assign, real Acknex's "VECTOR = SCALAR sets .x" idiom) and `force.x`/`.y`/`.z` (read by `move_gravity2()`) lived in two disconnected storage locations (`_globals` vs `_vectors`) — a scalar write never reached the vector side, so `force.x` always read back as 0 regardless of what the script just set; fixed in `_set_var()` to mirror a scalar write into the vector store's own `.x`. Verified via new `smoke_town_traffic_check.gd`: a spawned traffic car now actually drives along its bound path (was frozen at its spawn point even after fix 1 alone). Full `smoke_dispatch --all` (55/56, unchanged) confirms no regression across the corpus from the `move()`/`_set_var()` changes, both hot, widely-shared paths. **Town: missing land**: investigated directly — every one of Town's 530 placed entities (including the WMB-block/brush-geometry ones like `CityWtr.wmb`, which live under a separate `assets/converted/wmb/`/`levels/*_brush.glb` path, not `assets/converted/mdl/`) resolves to a real, on-disk GLB; nothing is actually missing from the asset pipeline. Most likely explained by the same black-sky bug above — distant terrain silhouetted against a solid black sky reads as "missing" rather than rendered. **Pee animation still not working**: GB-17's own scoped-down particle system (generic fading/scattering sprites, since genuinely interpreting a particle function's per-particle WDL body is out of scope) rendered every `emit()` effect corpus-wide with the same hardcoded bluish-white tint, regardless of what the effect actually was — Smash's own `stream()` (Piposh's pee particle function) sets `MY_MAP = bpee;` (a real yellow-toned bitmap, `Pee.png`), never read before. Fixed by statically scanning the particle-action's own function body for its `MY_MAP = <bmap>;` assignment (a single top-level statement, not runtime behavior — read the same way `wdl_meta.json`'s own static extraction already works elsewhere) and resolving it through the same `_resolve_bmap_texture()` every panel/HUD bitmap already uses; falls back to the old generic dot for any particle action with no resolvable `MY_MAP`. Verified via new `smoke_particle_texture_check.gd` (Smash's `stream` now resolves to `Pee.png`, an unrelated bogus action name still safely falls back). Applies to every `emit()` call in the corpus (smoke trails, lava, blood, gun brass, ...), not just pee. **Plane wings, re-investigated with the actual screenshot**: re-checked both leading theories from GB-19's own re-opening. (1) Camera-authority switch (FP player camera vs. the WDL-scripted chase camera, `docs/CONTRACT.md` §4.1.1): confirmed working correctly via a live headless trace — `action CamPlane`'s own per-tick `camera.x/y/z/pan/tilt/roll` writes are correctly detected same-frame and the script camera does take over from the player's FP camera for the whole Scene==2 sequence (an earlier trace attempt wrongly suggested otherwise, traced to a bug in the diagnostic script itself — it held a stale reference to whichever camera was active at the moment it was fetched, not a live "which camera is current now" query). (2) The chase camera's own `vec_to_angle()`-driven look-at computation is genuinely correct and matches real Acknex's documented GS pan/tilt convention — and it produces a real, dramatic, SCRIPTED tilt swing (-71.8° looking steeply down at the plane near spawn, leveling out to +10.3° by 10 seconds in, as `action CamPlane` and `action B747` drift apart in position every tick per their own WDL bodies) — not a bug, but the level's own authored cinematography. With the model mesh (GB-19), the camera switch, and the camera's own angle math all independently verified correct, this doesn't reproduce as a fixable bug at any layer checked so far. Left open: still unconfirmed whether the specific FRAMING this produces (a steep top-down angle early on, when the plane is relatively close) is what the screenshot actually shows, since headless Godot still can't render a frame to compare directly — would need either an in-game screenshot taken a few seconds into a fresh playthrough of this exact moment, or a description of which part of the 10-second sequence the attached screenshot was taken from. |

| GB-21 | Follow-up (2026-08-10): "the background in some places is still the wrong asset"; "there's a cyan colored sky in the background where Piposh falls, that swaps to a cloud background and back, it should be the cloud"; "there are layers missing... a vastly different background and sky for scenes and images that are not seen in the back that should be." | Two of three root-caused and fixed; the specific PiposhFall cyan/cloud swap investigated but not conclusively reproduced. **Wrong asset (corpus-wide)**: `tools/extract_wdl_meta.py` never stripped `//`/`/* */` comments before scanning for `bmap` declarations — `AfterRac.wdl` has a commented-out `//bmap sky = <GOlfSKY.pcx>;` that still matched, poisoning the single shared `bmaps` dict this script mutates across every file in scan order. `Desert.wdl`, `Final.wdl`, and `Intro3.wdl` all resolve their own real `SKY_MAP = sky;` assignments against that poisoned entry (scanned before `WDL/Weather.wdl`'s own correct `BMAP sky = <sky.pcx>;` declaration, too late to fix them), silently swapping in a fully-opaque, wrong-looking sky (a light cyan sky with soft white clouds) in place of the real one (99.9% transparent, a few star-dot pixels). Fixed by stripping comments before every regex scan; regenerated `wdl_meta.json` — those three levels now correctly resolve to `sky.png`. **Missing layers / "not seen in the back"**: found that `sky_map`/`cloud_map` were never live-polled at all — only `scene_map` had a live-value hook (added in GB-18), and even that was bounded to a 300-frame post-load window. Several real, correctly-interpreted WDL effects change `SKY_MAP`/`CLOUD_MAP` for the rest of a level's own runtime (`Ziggy.wdl`'s own per-wave `SetWeather()`, re-dispatched every time the player clears a wave; `Desert.wdl`'s Mansion-stage storm; `Intro3.wdl`'s storm; `Mount.wdl`'s snow — all via the shared `WDL/Weather.wdl` library) — the interpreter computed all of this correctly the whole time, but none of it ever reached the actual rendered sky. Generalized the scene_map-only live-value hook into `_live_bmap_file(var_name)`, used continuously (for the level's whole lifetime, not a bounded window) for all three of `scene_map`/`sky_map`/`cloud_map` — three cheap string reads per frame, only rebuilding the sky when a value actually changes. **PiposhFall cyan/cloud swap**: traced the report to the wrong level at first (Plane3's `action PipFall`, a similar-sounding but unrelated sequence) before finding the real one, `Smash.wdl`'s `action PiposhFall`. Confirmed via live trace that Smash's own `scene_map` resolves and stabilizes within the first few frames of level load and never changes again — ruling out a scene_map/sky_map flicker as the cause for this level specifically. The fall sequence itself is gated behind a multi-stage trigger chain (`action Genia` walking up to the player, setting `player.skill2`) that a synthetic headless trigger couldn't fully replicate in a way that reproduced camera movement — the most likely remaining explanation is the same class of thing already found and explained for Plane2's takeoff camera (GB-20): a scripted camera actively swinging its own view angle in a way that reveals more/less of the horizon-cylinder silhouette vs. open sky as it cuts between Smash's several "Cam"/"Cam2"/"WartCam" shots, not a background-swapping bug — but this is not conclusively confirmed, and remains open pending a specific timestamp or a repro closer to the actual trigger sequence. New regression test `smoke_live_sky_poll_check.gd` verifies the live-poll path end to end (forces a mid-level `sky_map`/`cloud_map` change and confirms it's picked up, simulating what `Weather.wdl`'s own functions do). |
| GB-22 | Same report (2026-08-10): "the lighting effects are vastly different than the ones from the original game." | Partial, first-pass fix — see NB-3, which this doesn't fully close. The port had zero directional light sources anywhere: every level was lit purely by flat ambient (`level_runner.gd`'s own fixed color/energy) plus WMB-placed `OmniLight3D` point lights, with shadow casting/receiving forced off on every mesh — structurally flat, no directional shading anywhere, not a per-level tuning issue. Acknex does have a real directional "sun" concept (`WDL/lflare.wdl`'s own `sun_pos`, read by every level's lens-flare code) but no script in this corpus ever assigns it a value, so there's no live per-level direction to read back and match. Added one generic `DirectionalLight3D` (fixed angle, warm-neutral color, moderate energy tuned to complement rather than override the existing ambient/point lights) — shadows deliberately left off, since enabling them is a separate, higher-risk change (performance + visual quality on this corpus's low-poly models) that needs real visual verification, not a blind guess. This is a structural "add the missing light class" fix, not a tuned match to a specific reference — headless Godot can't render a frame to compare against the original, so this is flagged honestly as a first pass, not a confirmed resolution. |

| GB-23 | Follow-up (2026-08-10), with three side-by-side screenshots (two from this port, one from the original) of the exact same in-game moment (Town, the Tofu stand): "the clouds had texture and pattern on them, when Piposh falls there's fog... there is light and shadows that don't exist in the current graphics, theres 'ground' that spreads when piposh hits the ground during the smash fall scene, and the pee still isn't 'flowing' in the right angle, and there's an animation that should run on a big screen in the smash WDL that's just a single picture now." | Four of six fixed, root-caused via direct screenshot comparison rather than guessing. **Diagonal-tiled horizon (the big one)**: the port's sky/horizon showed a broken, diagonally-sheared repeating pattern instead of the original's coherent factory skyline — confirmed the source texture itself (`horizon1.png`) is byte-identical in intent to the original (a single coherent skyline with chimney smoke), so the bug was purely in how it's mapped onto the horizon cylinder. Dumped the raw UV array from Godot's own `CylinderMesh` directly and found each horizontal ring has a DIFFERENT U phase offset than the ring above/below it — a spiral/shear baked into Godot's own primitive-mesh UV generation (apparently meant for a genuine cone taper, degenerating into a uniform-but-nonzero per-ring twist even with equal top/bottom radius). Combined with the existing `SCENE_REPEAT=6` texture-tiling, this shear is exactly what read as diagonal banding. Fixed by replacing `CylinderMesh` with a hand-authored `ArrayMesh` (`AcknexSky._build_scene_cylinder_mesh()`) whose UVs are computed directly — a clean, non-sheared wrap regardless of Godot's own primitive-mesh internals. This is the single highest-impact visual fix of the whole session — it affects every level's own horizon cylinder, not just Town. **Fog**: `camera.fog`/`fog_color` (11 corpus levels, including Smash's own `main()`) were entirely unbridged — `_set_camera_field()` had no "fog" case at all. Fixed: stored as camera meta on write, applied to the real `WorldEnvironment`'s depth fog via a new continuously-polled `_apply_wdl_fog()` in `level_runner.gd` (same live-poll shape as sky_map/cloud_map/scene_map). The exact Acknex-value-to-Godot-fog-distance mapping is a reasoned guess (headless Godot can't render a frame to tune it against a reference), flagged as such. **Pee flow angle**: the particle system's own generic random-burst direction (a reasonable default for splash/scatter effects like blood or sparks) was wrong for a genuinely DIRECTIONAL emitter — Smash's own `function stream()` computes a real, evaluated initial velocity (`MY_SPEED.X=-PeeStr+jitter; MY_SPEED.Y=PeeStr+jitter; MY_SPEED.Z=PeeStr+jitter;`, all particles flying in roughly the same direction, which is what makes it read as a stream). Unlike the earlier `MY_MAP` texture fix (a static AST read), this expression depends on a live global (`PeeStr`, which decreases over time) — fixed by evaluating the found `MY_SPEED.X/Y/Z` expressions via the interpreter's own `_eval()` at the moment `emit()` fires, using real current global state, and blending that direction with a smaller jitter term instead of the old fully-random burst. Falls back to the old random-burst behavior for any particle action with no `MY_SPEED` assignment found (smoke/lava/blood/debris-style effects unaffected). **"Big screen" showing a static picture**: traced to Smash's own wart mini-game (`DialogChoice==3`); the wart-counter HUD panel (`pWart.bmap=Wart1..7`) already worked correctly (confirmed directly), but the animated wart CREATURES themselves — spawned via `create(<Wart.pcx>,camera.x,Warty);`, a real, active corpus usage of Acknex's "spawn a billboard sprite from a bitmap, not a full 3D model" `create()` idiom — silently failed every time, since `_do_create()` only ever resolved its 1st argument against a converted `.glb` model. Fixed generically: when the `.glb` resolution fails, `_do_create()` now tries resolving the same stem against `assets/converted/gfx/` and spawns a real billboard `Sprite3D` if found, then continues through the same generic entity-field/action-coroutine machinery as an MDL-based entity. Not a Smash-only fix — `WDL/doors.wdl`, `WDL/venture.wdl`, and `WDL/war.wdl` all use the identical shape (currently unreached by this game's actual level scripts, but no longer silently broken if they ever are). **Still open**: "ground that spreads when Piposh hits the ground" — no corresponding `emit()`/dust/decal/screen-shake code found anywhere near `action PiposhFall` or its own landing transition in `Smash.wdl` (the one `emit()` call near it, in `action Vespa`, is the scooter's own unrelated exhaust trail) — not chased further without more specific detail, since guessing at an entire new visual mechanism without a concrete WDL anchor risks being unhelpful. **Shadows/directional lighting depth**: GB-22's own first-pass directional light (added same day) is a structural start, but real shadow casting is still off everywhere (`_force_unshaded_if_needed()` still forces `cast_shadow=OFF`/`disable_receive_shadows=true` on every mesh) — enabling that is a separate, higher-risk visual/performance change still deferred, tracked under NB-3. |

| GB-24 | Follow-up (2026-08-10): "the fog is not implemented well. One pattern added to the background looks better but the weird clouds are still the wrong pattern... make the game work like it was originally, system-wide." User pointed at an existing, already-working reverse-engineering environment (`E:\RE_general\PiposhTools\piposh_3d_cursor`, a checksum-cracked, dgVoodoo2-wrapped copy of the original engine) and explicitly authorized using it to get real, live reference renders instead of reasoning from textures alone. | Both root-caused against a real, live capture of the actual original engine running (3D GameStudio A5, engine build V5.240 — confirmed directly from its own boot banner). Launched `Town.exe`/`Smash.exe` via that project's own `-d l1 -NX 512` flags; exclusive-fullscreen mode kept crashing within ~10-15s in this environment (a known class of DirectDraw-under-remote-session issue), so `dgVoodoo.conf`'s `FullScreenMode` was switched to `false` (backed up first) to get a stable windowed capture instead. Got a clean, full-quality live screenshot of the exact Tofu-stand scene the user's own earlier screenshots showed. **Sky/cloud pattern**: direct comparison showed two real, separate bugs in `_make_sky_panorama()`, not one — (1) the whole 320×320 `clds.png` cloud shape was being stretched as ONE pass across the FULL panorama width with no tiling at all (`cx = x * cloud_img.get_width() / w`), warping a single cloud silhouette into a grossly distorted ~4.5×-wider-than-tall smear across the entire sky, where the reference shows several distinct, naturally-proportioned soft puffs with open sky between them; (2) `clds.png`'s own opaque pixels are almost pure black (confirmed via direct pixel read, dominant values 1-17 out of 255) — alpha-blending that literal RGB paints near-invisible dark smudges, not the soft white puffs the reference clearly shows. Rewrote cloud placement to tile the texture at its own native square aspect ratio in a small number of well-separated cells (each shrunk to leave real clear sky around it, not edge-to-edge), and treat the texture as a shading/density mask blended toward a soft white tint (its own alpha for silhouette, its own luminance modulating internal opacity) rather than a literal paste color. Also darkened/enriched the base sky gradient (a rich blue-purple, matching the reference's own genuinely nighttime sky) — the previous version was a much paler, washed-out blue. **Fog color**: previously left unapplied on the theory that `fog_color`'s corpus-uniform literal value of `1` was unused boilerplate — reconsidered after confirming (via the same live reference) that this is a genuine NIGHT scene: per GB-20's own "VECTOR = SCALAR sets only .x" finding, `fog_color=1;` means (red=1,green=0,blue=0), i.e. very close to pure black in the same roughly-0-255 scale `sky_color.red` uses elsewhere in the corpus — exactly the sensible, deliberate choice for fog that fades distant geometry into night darkness rather than a daytime gray haze. Now reads the real live `fog_color` global and applies it as the actual fog tint (falling back to neutral gray only for the rare level that sets `camera.fog` without ever touching `fog_color` at all). The `camera.fog`-value-to-fog-distance mapping itself is still an unverified, reasoned guess — the one live capture obtained was of a no-fog default state, not a moment with fog actively visible, so there was nothing to tune that specific number against yet. |

| GB-25 | Follow-up (2026-08-10), after live-verifying GB-24 via non-headless capture + a fresh dual-capture against the real original engine again: "now the plane3 level is really dark... And the Smash level still has the weird cloud artifacts... The water doesn't look the same like the original... the ground pattern when we reach the town level is pixelated instead of a gradient." Then, in the same session, a second follow-up: "the ground in the town view should be gradient not pixelated [still]... misalignment between the water, the driving cars etc that they are not in the same heights... The fog issue still occurs. The lighting effects from the original game are missing." | Six real, independently-verified root causes, all fixed. **Cloud "wallpaper" artifact (Town/Smash)**: root-caused via systematic isolation (hid `AcknexSky`'s own cylinder, then forced a flat `BG_COLOR` background — pattern persisted through both) to real WMB BRUSH geometry, not anything in `AcknexSky` at all. A genuine, corpus-wide Acknex/Quake-family convention: brush faces meant to be an infinite-distance backdrop are marked by texture name, not a flag — confirmed three real naming conventions in the actual WMB data via a full corpus scan of every level's own `_brush.glb` materials: "sky\*"/"z\*SKY\*" (skywhite, skyblue, zSKYNEW), French "ciel" (CIEL, CIELIN, mansion_ciel — this game's own text mixes English and French), and a literal blank texture name in the WMB texture lump (Godot's glTF importer labels these "#default"; Town/Desert/MOI all seal the level with exactly this kind of unnamed ceiling face, confirmed via a physics raycast fanned out from the live camera at every pitch 0-89° hitting real collision geometry, never empty sky). All three now get a transparent (`ALPHA_SCISSOR`, not plain `ALPHA` — see below) override material in `WmbLevelLoader`. Two wrong turns before landing on the right check, both left as comments for the next person: first tried matching the extracted PNG's own filename (`begins_with("sky")`) — never matches, since these textures are runtime-loaded straight out of the level's own packed `.glb`, where `Texture2D.resource_path` is an internal glTF-packed path, not a filename; the real signal is the glTF *material's own name* (`mat.resource_name`), confirmed live via a headless dump. **Sky rendering flat/no stars despite a correct panorama**: even after the brush fix, Town's sky rendered as a flat, featureless wall of color — headless verification confirmed `_make_sky_panorama()`'s own `Image` had the correct gradient + star + cloud content, so the bug was in the GPU sky pipeline, not the data. Fixed by setting `Sky.process_mode = PROCESS_MODE_REALTIME` (`PROCESS_MODE_AUTOMATIC`'s importance-sampled convolution, meant for physically-plausible rough reflections, washes out this panorama's sparse near-single-pixel star dots into near-invisibility — not wanted for 2D stylized backdrop art). **Ground pixelation ("still" happening after an earlier same-session filter fix)**: the earlier fix (`TEXTURE_FILTER_LINEAR_WITH_MIPMAPS` for brush/terrain surfaces) was necessary but not sufficient — confirmed headlessly (`bm.albedo_texture.get_image().has_mipmaps()`) that the underlying `Image` had NO actual mip chain, since brush textures are loaded straight out of the runtime glTF-embedded buffer, which never builds one the way Godot's own on-disk `.import` pipeline does. A filter MODE with no real mip levels falls back to the base level only — for a small (128×128 on Town's own ground) texture tiled many times across a large ground plane and viewed at a grazing/distant angle, that's the exact texture-minification case mipmaps exist for; without them it aliases into a hard, blocky moiré pattern. Fixed by generating a real mip chain for the image once and rebuilding the texture from it. **Traffic cars not at a consistent height ("the water, the driving cars etc... not in the same heights")**: every WED-placed entity gets its own model's feet-to-origin offset corrected at spawn (`WmbLevelLoader._snap_mesh_feet_to_origin()`), but a runtime `create()`'d entity never went through that spawn path at all — and this is exactly how every piece of moving city traffic comes to exist (`action MakeCars`' own `create(<car.mdl>, waypoint, SportCar)` calls, real corpus usage in Town/Fight/Race/Mount/Mine). Confirmed live via the `[feet-snap]` debug log: four different traffic-car models spawned at the identical shared waypoint height (349.174) each needed a different correction (TownCar2 → 421.09, BusiCar2 → 412.89, TownVesp2 → 413.77, Bus32 → 421.17) — exactly the inconsistent-height symptom reported, invisible for any WED-placed car nearby (already corrected) but obvious for moving traffic. Fixed by reusing the loader's own identical opt-out gate/correction for every non-sprite `create()`. **Fog still "occurring" wrong (Plane3 "really dark")**: the same-session fog-distance-scaling fix (anchor `fog_depth_end` to the level's own real bounding-box diagonal instead of a fixed constant) was a real improvement but didn't fully explain the report — found a second, structural bug: Godot's `Environment.fog_sky_affect` defaults to 1.0, blending the sky/background itself toward `fog_light_color` as if it sat at the fog's own far depth. Classic DirectX fixed-function depth fog (what this game's real A5 engine used) exempts the skybox entirely by design — there's no real depth to fog a backdrop against. With `fog_color=1` (near-black, GB-24's own finding) active for Plane3's whole aerial fall sequence, `fog_sky_affect=1.0` washed the ENTIRE sky — dominating an aerial shot — toward solid black regardless of any distance-formula tuning, since the sky is conceptually at infinite distance and always exceeds any finite fog range. Zeroed to match the original engine's own behavior (fog affects real world geometry only). **Missing lighting/shadows** (also NB-3): shadows were forced off everywhere, unconditionally, on every light and every mesh — a structural gap, not a tuning question, previously deferred pending live visual verification that never happened. Turned on: the one directional "sun" light (GB-22) now casts (one shadow map, cheap regardless of scene size); every WMB `OmniLight3D` now casts too (corpus-wide light counts are small, 0-6 per level, confirmed via a scan — cheap); every brush/prop `MeshInstance3D` now both casts and receives. Had to switch the sky-brush invisible material (above) from plain `TRANSPARENCY_ALPHA` to `TRANSPARENCY_ALPHA_SCISSOR` as part of this — shadow casting is a whole-`GeometryInstance3D` property, not per-surface, so a plain alpha-blended "invisible" surface sharing a mesh with real opaque ones (Town/Desert/MOI's own ceiling-plus-walls shape) risked still contributing a giant, level-covering shadow from its own un-discarded depth-pass geometry; a scissor discard is excluded from both the color AND shadow depth pass, guaranteed. Verified via new `smoke_sky_brush_transparency_check.gd`, an extended `smoke_fog_check.gd` (now also asserts `fog_sky_affect==0`), and the full existing regression set (`smoke_dispatch` 19/19, `smoke_town_traffic_check`, `smoke_click_survey` 16 levels/307 clickables/zero script errors, `smoke_smash_visuals_check`, `smoke_plane3_dome_scale_check`) all green. **Water vs. original, and general click-survey audit**: live-compared this port's own Town water directly against a fresh capture of the real original engine (same `piposh_3d_cursor` dgVoodoo2 environment as GB-24) — already a close match (flat, saturated blue in both; the original has no visible wave/reflection shader either). Ran the full 16-level click survey as a general audit for "other issues when clicking on other stages" — 307 clickable entities exercised, zero script errors, nothing further found without a more specific repro. |

| GB-26 | Follow-up (2026-08-11): "pressing on the Inn to start the level just shows a dark screen instead of showing the Inn"; "Starting the Taxi level doesn't show Piposh model on screen, I think it's a system-wide issue for parsing the WDL and WMB files and not a singular Taxi level issue." Then, after the first fix landed: "there's no Piposh character in the Inn level, so what you did to fix was partial, and there's a black ball floating statically instead." | Three real, corpus-wide bugs, all fixed — see this file's own commit message / `docs/SESSION_LOG.md` for the full trace. **Inn black screen**: root-caused via a live reference capture of the real original engine (`Inn.exe`, same `piposh_3d_cursor` environment as GB-24/25) to a camera-marker entity (`Camera.MDL`, `action Watch` — an always-active, ungated `while(1){camera.x=my.x;...}` pattern used corpus-wide) getting wrongly feet-snapped (its stem "camera" wasn't covered by any of this port's camera-placeholder checks, which only ever matched the exact stem "cam") into solid ceiling geometry — camera embedded inside a mesh, solid black. Confirmed the real engine shows a normal lit ceiling from the same camera. Broadened every camera-placeholder check (feet-snap exclusion, mesh-hiding, FP collision skip) to a shared `_is_camera_stem()` helper covering "cam" and "camera" both — a corpus scan found 12 levels (Inn/Taxi/Smash among them) use `Camera.MDL`. **Black ball instead of Piposh**: `Dummy.MDL` is reused corpus-wide under dozens of different custom WED-assigned action names (Inn's own RightEye/Quit/Flash/LimitIt, Credits' Torch, InShrine's Light, Mine's Gayser, ...) — the existing "hide this generic placeholder sphere" check only matched entities whose action was literally named "Dummy", missing every one of these; now checks the stem too. **No Piposh visible**: the WED-placed player body is hidden once, unconditionally, at spawn for every first-person level (correct for genuine FP gameplay) — but Inn's own `action Watch` is a scripted third-person establishing shot before normal walking begins, where an external viewer should see Piposh. `CameraAuthority` now toggles the body's visibility in lockstep with its own existing script/FP camera arbitration instead of a one-time spawn-time decision. Taxi's own reported "no Piposh" wasn't reproducible after the camera-stem fix (live-captured both of its own Cam-driven angles, Piposh visible riding the taxi in both) — most likely the same root cause, since Taxi is also in the `Camera.MDL` corpus list. |
| GB-27 | Follow-up (2026-08-11), full playtest pass across ten reports: (1) "the Plane3 background is dark, like the background itself"; (2) "the plane2 lighting is a bit too dark... but it does have some lighting"; (3) Plane2 takeoff wings "under the floor" until airborne; (4) Smash "doesn't have lighting like the orig game" + no ground-spread on impact; (5) Smash "big screen with yachdal video" still a static picture; (6) pee "still doesn't have the same path of flow"; (7) Map level "background that doesn't seem related"; (8) Town car/water heights "still not correct"; (9)/(10) covered by GB-26. | Two root-caused and fixed (7, 6); one partially improved with a real, documented remaining gap (8); the rest need a live re-check that a mid-session Windows session lock cut short (see `docs/SESSION_LOG.md` for the full process note) or more specific info. **Map background mismatch (7), fixed**: confirmed directly that `Map.wdl` never calls `load_level()` at all — it's a pure panel/UI screen (`pMap`'s own world-map artwork plus location-icon panels), the same shape as Menu (already treated as indoor/flat). With no real 3D level loaded, the default sky dome + generic `horizon.png` factory-skyline backdrop was still showing full-screen behind the 2D map artwork. Added "map" alongside Menu/Credits/Studio/VilEnd in `AcknexSky`'s own indoor list. **Pee flow angle (6), fixed for real this time**: GB-23's own fix correctly read the real, live-evaluated `MY_SPEED.X/Y/Z` direction from `stream()`, but only ever applied the GS→Godot axis remap — never rotated it by the emitting entity's own pan/tilt/roll, even though `MY_SPEED` in Acknex's particle model is entity-relative, the same way `move()`'s own `dist` argument is (already fixed for that exact reason earlier this session, GB-20's own `_do_move_call()`). Without the rotation step, the stream always pointed the same fixed WORLD direction regardless of which way Piposh was actually facing during the scene. Now rotates by `my`'s own `_acknex_entity_basis()`, identical to `_do_move_call()`. **Town car/water heights (8), real improvement confirmed, real gap remains**: dumped live positions after GB-25's own traffic-car feet-snap fix — WED-placed reference cars (`action PatrolCity`) sit at Y≈314-316, but runtime-`create()`d traffic (`action SportCar`, the same fix from GB-25) now sits at Y≈412-421, ~100 units higher. Traced the gap to two stacked, only-partly-fixed issues: the shared traffic waypoint's own raw height (349.174) is already ~34 units above the WED reference before any correction at all (a path-data authoring quirk, not something this port's own code produced), and the feet-snap correction on top of that (65-72 units, model-dependent) may itself not be the right correction for a path-driven vehicle specifically — real Acknex path-follow may already assume the waypoint's own Y is the correct visual height, with no further per-model correction wanted, unlike a WED-placed static prop. Left as-is (a confirmed net improvement — cars are no longer ALL pinned to one uniform wrong height — over a further blind guess at the correction), flagged honestly rather than re-guessed without a live render to tune against. **Plane3 dark background (1)**: reproduced live and confirmed via the game's own debug camera-force hooks (new `tools/visual_check.gd` `stage=`/`scene=` modes) — background renders solid black during the Stage>1 fall sequence despite every checked value being correct headlessly (panorama gradient data intact, `fog_sky_affect=0`, `fog_enabled` correctly true, sky material stable — rebuilt only once, ruling out a radiance-bake race). Investigation was cut short by the machine's own Windows session locking mid-capture; the exact GPU-side cause is still unconfirmed. **Plane2 takeoff wings (3)**: pursued a live close-up camera override (`tools/visual_check.gd`'s own persistent watcher-node technique, since the interpreter's own scripted camera re-asserts control every frame and silently undoes a one-time override) — the B747's own mesh AABB (2824×650×2996 units) is large enough that both a 600-unit and a 3200-unit camera offset from its center still landed inside or immediately adjacent to its own geometry, never producing a clear, unobstructed framing to actually inspect the wing/floor relationship. Not reproduced conclusively either way this round. **Plane2 lighting (2)** and **Smash lighting/ground-spread (4)**: GB-25's own corpus-wide shadow/lighting turn-on already applies to both (same shared code, not per-level), but neither has been re-confirmed live against this specific "still a bit dark" / "ground-spread on impact" framing since that fix landed. **Smash "Yachdal" big screen (5)**: `Yachdal.MDL`/`action Yachdal` genuinely exist in this corpus (a reusable crowd/spectator action) but only in Cardgame/Intro10/Mansion — confirmed via a full-corpus grep that neither the string "Yachdal" nor the model appears anywhere in `Smash.wdl` or Smash's own level data at all. GB-23's own "Smash big screen shows a static picture" fix (the Wart-creature `create()`-as-billboard-sprite bridge) is still in place, unregressed — but given "Yachdal" doesn't belong to Smash in the source data at all, this report may be describing a different level's own big-screen moment (Start's own crowd scene is the corpus's actual Yachdal usage) misattributed to Smash while playtesting in sequence; needs clarification on which level before chasing further. |
| GB-28 | `zero-py-no-assets` branch, follow-up (2026-08-11): "also, the poster behind Ami in the Studio room now doesn't show an image, but just a blank paper instead." | Fixed. `_mount_wall_card()`'s "shiknote" branch hardcoded `res://assets/converted/wmb/ShikNote_modaa1.png` — a path that only ever existed under the old Python-pipeline output, deleted on this branch along with the rest of `assets/converted/**`. `ResourceLoader` silently failed to find it and fell back to a flat tan placeholder colour, i.e. "blank paper." The real texture was already being loaded correctly elsewhere on the same entity (the runtime WMB reader's own degenerate-geometry brush mesh carries the right material) — fixed by reusing it directly via a new `_find_first_albedo_texture()` helper instead of re-deriving a dead converted-asset path. |
| GB-29 | `zero-py-no-assets` branch, follow-up (2026-08-11): "the screen behind yachdal is lit up, but its not like that in the orig game, and there was light froming from some light sources which is now not seen in the game, it just looks a bit dark, i think this issue is the same for all stages in the game." | Root cause found and fixed: Acknex lets any WDL entity act as its own dynamic point-light via `MY.LIGHTRANGE`/`LIGHTRED`/`LIGHTGREEN`/`LIGHTBLUE`, independent of the entity's own mesh visibility (confirmed via `ACTION FlickerLight` in Intro7.wdl/Intro12.wdl, which sets `MY.INVISIBLE=ON` on itself then drives exactly these four fields; used across 24 corpus files). This was entirely unbridged in `_set_field()` — every write to these four fields was a silent no-op — so every scene that relies on a scripted entity-light for local contrast (not just uniform ambient) rendered flatter and darker than the original, matching the user's own "same issue for all stages" instinct. Fixed by creating a real `OmniLight3D` child on first write (`_get_or_create_entity_light()`) and driving its range/color from the four fields, stored under the same `wdl_custom_<field>` meta convention `_get_field()`'s generic fallback already reads, since several corpus scripts (HitUFO, InShrine) read these fields back incrementally to animate flicker. |
| GB-30 | `zero-py-no-assets` branch, follow-up (2026-08-11) while re-investigating item 8 from GB-27 ("Town car... heights"): "there are no running cars" reproduced fresh on this branch. | Found a much bigger bug than the one being chased: `_do_create()` (the bridge for WDL's `create()` builtin — `action MakeCars`' own traffic spawns, but also every gib/debris/spark spawn corpus-wide) still hardcoded `res://assets/converted/mdl/<Stem>.glb`, a path family this branch deletes almost entirely. Every runtime `create()` call silently found no model, fell through to a bitmap-sprite fallback, and returned nothing at all when no matching GFX texture existed either — so `action MakeCars` never spawned a single visible car, all 400+ frames of its own spawn loop running for nothing. Not Town-specific: any level whose `create()` calls spawn a real 3D model (not a billboard sprite) was affected. Fixed by mirroring `WmbLevelLoader`'s own SEAM 3/4 branches here — read the original `.MDL` via `_read_runtime_mdl()`/`_build_runtime_mdl_node()` and attach animation via `_attach_animator()`, the exact same runtime-MDL code path a WED-placed entity already uses, so a `create()`'d entity and a level-placed one resolve identically. Verified live: `smoke_town_traffic_check.gd` now spawns a Bus3 and drives it 126 units in 120 frames (previously: no car ever spawned). `smoke_bitmap_create_check`, `smoke_gib_debris_movement_check`, `smoke_plane3_vase_catch_check`, `smoke_range_hitscan_check`, and `smoke_dispatch` (19/19) all still pass. |
| GB-31 | Follow-up (2026-08-11): "There's still no light sources that are 'bulbs' or 'projectors' in it, i can clearly see even on the first scene in the intro. The background behind yachdel, is a poster, that supposed to be just static but it emits light and is flashing where it shouldn't be." | One real, concrete bug found and fixed; the rest needs a live screenshot to pin down further. Traced "the first scene in the intro" to `Start.WMB` (the game's actual opening level -- confirmed via its own entity list: `Yachdal.MDL` plus 5 `Slamp.MDL` stage-lamp fixtures and 6 native WMB point lights all live in this one scene together). **Fixed**: GB-29's own new `_get_or_create_entity_light()` left `shadow_enabled = false` -- the one inconsistency with `_spawn_light()`'s own established convention (native WMB point lights already turn shadow casting ON, specifically because corpus-wide light counts are small enough that real occlusion is cheap). An unoccluded light shines straight through solid walls exactly like sunlight with no glass, so any scripted light (`ACTION FlickerLight`, GB-29's own subject) could light up something on the far side of a wall it was never meant to reach -- matches "emits light... where it shouldn't be" as a class of bug, even though `Start.wdl` itself doesn't happen to use the LIGHTRANGE fields GB-29 bridges. Now matches every other light in the engine. **Not yet root-caused**: confirmed both `Slamp.MDL` (flags=0, not hidden) and `Yachdal.MDL` spawn correctly with valid geometry, and confirmed no WDL script in `Start.wdl` touches lighting, camera flash, or fade effects at all -- so the "flashing" is very likely a rendering-level artifact (shadow-acne/z-fighting, a missing emissive channel on a "glowing bulb" skin texture, or wrong-speed skin/frame cycling on a nearby animated model), not a scripting bug, and none of these are safely distinguishable without seeing it. Needs a screenshot or short clip before chasing further. |
| GB-32 | Follow-up (2026-08-11), Plane2, found via the dev scene inspector: "The Headphon_mdl_007 is rendered bigger than it should be and it is bigger than Piposh's head where it should be in a smaller size." User's own follow-up correctly rejected a scale-based fix: "this headphone... might be correctly sized, it might be [in]correctly placed -- lower than it should be on Piposh's head... if it was in the correct place the size would seem right." | User's diagnosis was right, confirmed by the numbers. Ruled out a scale bug directly: this headphone's own authored scale (0.533) was verified byte-for-byte against the raw WMB file, and a full corpus scan found it's the ONLY `Headphon` placement anywhere using a non-`HeadPhone` action (`A1`) -- not a parsing error. The real cause: this one headphone is WED-authored as a fixed absolute offset from Piposh's own RAW origin (86 units above it), correct only if Piposh renders exactly at that raw origin. But Piposh DOES get feet-snapped in this scene (`_should_feet_snap` has no exclusion for `piposh`), which lifts it ~58 units for this placement -- while headphones are unconditionally excluded from feet-snap (needed so the level's other six, decorative, desk-resting headphones don't get incorrectly re-lifted). Piposh moved; the worn headphone didn't; they drifted apart, and from most angles the headphone ends up overlapping/sunk into the head rather than sitting on top of it -- reading as "too big" rather than "too low." First fix special-cased this one relationship by level/model/action name and was correctly rejected: "i don't want you to patch things per-level, it should be the general parsing for all." Replaced with a real engine-wide mechanism, `_couple_unsnapped_props_to_snapped_actors()`: exposes the feet-snap correction generically as `feet_snap_delta_y` meta on any snapped entity, then for EVERY level, groups all entities by their `action` name (any two entities sharing an action are almost always the same WED-authored "moment") -- if exactly one member of a group got a real feet-snap correction and another nearby (within 150 units) didn't, the unsnapped one gets the same correction, preserving whatever relative offset WED authored. No hardcoded level, model, or action name anywhere in the rule itself. A sample corpus scan (20 levels) confirmed it fires exactly once for this Plane2 case and stays inert everywhere else (ambiguous groups like Town's `PatrolCity` traffic or Race's hundreds of `Wheel`/`Tree` props, which have multiple snapped members, are correctly left alone). Verified live: headphone Y relative to Piposh's Y is still exactly 86.0 after the rewrite, matching the raw WED-authored relationship bit-for-bit. |
| GB-33 | Follow-up (2026-08-11), Plane2, found via the dev scene inspector: the B747 (`action=B747`, the scripted takeoff hero) sits with its own tarmac ground brush (material `metaldark4`) rendering in front of/above it, hiding parts of the aircraft. User explicitly rejected "it's scripted to climb into view, so this is by design" as an explanation and insisted this was a real parsing/positioning bug, not authored behavior -- correctly. | Confirmed by measurement. `B747.MDL`'s own local mesh spans Y -49.23..+43.32 -- at this level's authored scale (7.026x) and origin (Y=-82), the aircraft's own belly lands at world Y -427.9, roughly 288 units *below* the tarmac's own surface (`metaldark4`, Y -139.5..-212). "b747" (plus "biplane"/"biplane2") was on `_should_feet_snap`'s exclusion list under the theory that a WDL action later moving an entity at runtime (`action B747`'s `my.y = my.y + my.skill1 * time`; Intro4's `action Fly`'s `my.x += ..; my.z += ..`) meant its origin wasn't a real floor reference -- but this codebase already corrected the exact same mistaken reasoning once before, for "tv"/"hanger"/"towerw"/"cockpit" (see that removal's own comment, still in this function): a WDL action moving an entity later is orthogonal to whether its SPAWN position needs the same floor correction every other prop gets, since additive script movement (`+=`) just continues from wherever the corrected position starts. Confirmed this wasn't actually about runtime control at all: Plane2 places seven B747 copies, six of them with NO action at all (pure background scenery, no script ever touches them) -- all seven shared the identical wrong, uncorrected origin, so the six with no script were sitting wrong permanently, forever, with nothing to ever fix them. Removed "b747"/"biplane"/"biplane2" from the exclusion list (kept "island" excluded -- a huge terrain landmass, not a compact vehicle, a genuinely different failure mode). Verified live: all seven B747 placements in Plane2 now consistently render at Y=263.9 (belly at the original WED-authored Y=-82, ~58 units above the tarmac -- a plausible landing-gear clearance, versus 288 units *underground* before). Checked Intro4's biplanes too, no crashes. Full Plane2 smoke suite and `smoke_dispatch` (19/19) still pass; the two pre-existing unrelated failures in that suite (`smoke_plane2_move`, `smoke_plane2_afgcard_static`, `smoke_wmb_integration`) were confirmed via a stash/baseline comparison to predate this change. |
| GB-34 | Follow-up (2026-08-11), self-audit requested after GB-33 ("check other things you did wrong"): user found two more examples via the dev scene inspector -- `ShikNote_mdl_014`'s wall poster reported "also lower than it should be", then `BiPlane2_mdl_005`/`015` (Intro4, `action=Land`) reported "still not placed correctly and are under the floor" even after the GB-33 stem-list fix. | Two separate, real bugs, same root shape as GB-33 but neither fixed by it. **ShikNote poster**: `_mount_wall_card()`'s own vertical offset (`quad.position.y += quad.size.y * 0.12`) was a GUESS, explicitly flagged as one in its own comment since 2026-07-31 ("Unconfirmed pending playtest -- adjust or revert if still off") and never actually measured. Replaced with a real measurement: the entity's own (degenerate, edge-on) extracted brush geometry is still available one frame before `_hide_meshes()` removes it, so its real AABB center now positions the quad instead of a guessed fraction. Also ported the same wall-card special-case into the dev inspector tool itself (it had no equivalent at all, so it was showing the raw pre-replacement entity origin -- a tool gap, not a game bug, that read as "still wrong" on its own). **BiPlane2 (and other Land/Wind/item_pickup-actioned entities)**: GB-33 only removed the STEM-based exclusions (b747/biplane/biplane2); a completely separate ACTION-NAME-keyed exclusion list in the same function (`"land"`, `"wind"`, `"ent_rotate"`, `"item_pickup"`) survived untouched and kept catching entities by action regardless of stem. Audited each corpus-wide before removing: `"land"` -- Intro4.wdl's own `action Land` never touches position at all (just cycles an animation clip), so BiPlane2's Land-actioned placements had zero mechanism to ever move, permanently stuck; Plane3's `action Land` (the original reason for this entry, via "island") is already covered by island's own separate, still-kept stem exclusion, making this one redundant there and purely harmful everywhere else. `"wind"` -- Intro4's own `action Wind` only ever writes `my.pan` (rotational sway), never position; this exclusion never protected anything. `"item_pickup"`/`"ent_rotate"` -- grepped the entire corpus for both as action names and as any reference at all: neither has a matching `action` block anywhere, so any entity WED-assigned one of these runs no coroutine at all, ever (confirmed live, e.g. Plane2's own `BiPlane2_mdl_002`) -- same permanently-static shape as GB-33's six actionless B747s. All four removed; `"headphone"` kept (redundant with, not contradicted by, the existing `"headphon"` stem exclusion). Verified live: Intro4's Land-actioned BiPlane2 entities went from unchanged raw Y (~5-6) to real corrected values (~188-199); Plane2's item_pickup-actioned entities now also get corrected. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (16 levels, 307/307 clickables) both green. |
| GB-35 | Follow-up (2026-08-11): after GB-32's coupling fix, headphone still visibly floated above Piposh's head (screenshotted via the dev inspector's new Playwright integration -- the first LIVE VISUAL confirmation this session, not just numbers). User: "Read about the original acknex model parsing if needed to make it work correctly." | Root cause was one level up from GB-32's own fix: `docs/CONTRACT.md`'s own 2026-07-28 measured feet-snap audit had ALREADY flagged this exact stem as unresolved -- `headphon \| 46.3% below-origin \| doesn't move at runtime \| kept excluded... unresolved whether the stem-list entry is still needed on its own, low priority`. 46.3% is the identical bug shape to Cockpit (68.3%) and tv/hanger/towerw (32-55%), all four of which the SAME audit had already found needed the normal floor-lift once actually measured, not a blanket exclusion -- "headphon" was the one stem from that table nobody ever circled back to close out. GB-32's coupling fix was a reasonable general mechanism but was solving the wrong layer of the problem: it borrowed Piposh's own feet-snap delta instead of giving the headphone its own real correction from its own geometry. Fixed by narrowing the exclusion from the whole "headphon" stem to just `action == "HeadPhone"` (the six desk-resting placements, where a resting-on-furniture origin genuinely is a different reference point and re-lifting them was never verified safe) -- any OTHER placement (the worn one, `action A1`) now gets a real, independent feet-snap correction from its own measured mesh, same as everything else. This also makes GB-32's coupling mechanism naturally step aside for this pair (both members of the A1 action-group are now snapped, so its own ambiguity guard skips it) without needing to be removed -- it stays in place as a real, general fallback for any future corpus case with the same "one snapped, one genuinely can't be" shape. Verified with an actual screenshot this time: the headphone band now sits directly on top of the head with the earcups on the sides, not floating above it. The dev tool's own Python mirror of `_should_feet_snap()` had also drifted out of sync with the real engine THREE separate times this session (GB-33's stem removals, GB-34's action removals, and now this) -- each one read as "still broken" in the tool when the real engine had already been fixed; all now re-synced. Full regression: `smoke_dispatch` (19/19), `smoke_plane2_playtest`, `smoke_click_survey` (307/307) all green. |
| GB-36 | Follow-up (2026-08-12), confirmed live by clicking directly in the dev inspector: "The background part that's flashing is part of the brush walls floors" (material `intro_kraza`, Start.wmb -- the same one flagged unfixed in GB-33's own doc entry). Separately, live playtest of Plane2 found the player character completely unable to move (`smoke_plane2_move.gd`'s own pre-existing FAIL, now with a real root cause instead of just a confirmed-pre-existing baseline). | Both traced to the SAME underlying cause: `WmbFile.read_brush()`'s face list can contain multiple near-duplicate copies of the same real polygon -- not a parsing bug, genuine distinct face RECORDS in the source file, each producing its own real triangles. Measured directly: Plane2's own brush has 17,668 total faces, **4,044 of them (23%) are near-duplicates** of another face already emitted (same texture, same vertex count, centroid within 1 world unit, near-parallel normal). Two independent bad effects from emitting all of them: render-time Z-fighting (intro_kraza's own 3 stacked copies) and, worse, collision-time deadlock -- `_add_mesh_collision()`'s `create_trimesh_shape()` bakes every duplicate straight into the level's StaticBody3D, and a CharacterBody3D standing on ground with several redundant, coincident collision triangles can get `move_and_slide()` stuck reporting 6 simultaneous "collisions" (confirmed identical position/normal/collider, i.e. the same surface hit multiple times) with correctly-computed, non-zero velocity but zero net displacement -- looks exactly like "the keys don't work." Fixed with a spatial-hash dedup pass in `read_brush()`'s own face loop (grid-keyed by quantized centroid + texture, 27-cell neighborhood check -- a naive per-bucket linear scan was correct but O(faces²), measured at 30+ seconds for Plane2's own 17,668 faces before this; the spatial-hash version gives byte-identical dedup counts in ~1.2s). Confirmed via direct measurement: `intro_kraza` dropped from 166 to 88 vertices (a real, partial improvement -- the remaining two groups differ enough in position that they're very likely genuinely distinct geometry, not further z-fighting, so left alone rather than widening the tolerance on a guess). **Not yet confirmed to fully fix the Plane2 movement lockup** -- collision count alone may partly reflect `move_and_slide()`'s own normal multi-iteration behavior on a flat floor rather than duplicate geometry specifically; `smoke_plane2_move.gd` still fails after this fix and needs further investigation. Corpus-wide regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_playtest` all green; `smoke_sky_brush_transparency_check`'s pre-existing stale-node-path failure confirmed unrelated via baseline comparison. |
| GB-37 | Live playtest report (2026-08-12), Plane2: "1. no light source from places where in the light source (white round thing in the preview mode) is found / 2. The ceiling for the plane is not shown in the game now." | Root cause was `_force_unshaded_if_needed()`'s own sky/backdrop discard rule (`wmb_level_loader.gd`): alongside the genuine `"sky"` substring and blank/`"#default"`-name checks (both real, GB-history'd separately), a `mat_name.contains("ciel")` (French "sky") clause was added on 2026-08-10 on the unverified theory that every "ciel"-named texture -- Mansion's mansion_ciel/mansion_ciel2, MOI's CIEL, Shiks's CIELIN/shiks_ciel -- was the same class of real backdrop face, reached from the name alone, never by actually looking at a texture. Plane2's own ceiling uses `plane_ciel`, caught by the same rule and made fully transparent, which also explains item 1: the "white round thing" the user remembered from the WED preview is a row of cabin overhead lights **painted directly into `plane_ciel`'s own texture**, not a separate light-emitting entity -- hiding the ceiling hid the lights with it. Audited the whole claim properly this time instead of extending the guess further: extracted and visually inspected all 16 distinct "ciel"-containing texture names in the corpus (~20 levels) via their own `_brush.glb` exports. 14 of 16 are real, bespoke, hand-painted interior ceiling art, several with actual light fixtures drawn into the texture (Cieling2's recessed spotlights, MAINCIEL's stadium-light grid, shiks_ciel's and into12_ciel/into12_ciel2's bulb fixtures, Whitciel's recessed light, plane_ciel's own overhead strip, mansion_ciel/mansion_ciel2's sun-motif tiles, DUTYFREECIEL's and inn_corr_ciel's wood-panel ceilings) -- directly contradicting the 2026-08-10 comment's confident but unverified claim. Only `CIEL` (HitUFO/InShrine/MOI/Mount/Temple) and `CIELIN` (Shiks) are plain/generic enough to still plausibly be real shared backdrops, but nothing confirms that beyond the name either, so they were left as ordinary lit surfaces rather than allowlisted on the same kind of guess that caused this bug -- worst case some outdoor level shows a flat-colored ceiling instead of AcknexSky through an opening, a far smaller and safer gap than hiding 14 confirmed real ceilings corpus-wide. Fix: removed the `"ciel"` clause entirely, keeping only `"sky"`/blank/`"#default"`. Verified headless (`plane_ciel`'s material is now `transparency=0` with a real albedo texture, not the discard material) and via an actual in-game screenshot of Plane2's cabin ceiling, which now shows the real overhead light strip. Dev tool's own `isSkyMaterialName()` JS mirror re-synced. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307) both green. Items 3 ("the keys don't work -- I cannot walk around when it starts") and the Plane2/Inn/Taxi movement lockup from GB-36 remain open and are being investigated separately. |
| GB-38 | Follow-up (2026-08-13) to GB-36's own leftover: "**Not yet confirmed to fully fix the Plane2 movement lockup**... `smoke_plane2_move.gd` still fails." Root-caused properly this time instead of extending the duplicate-face theory further. | Traced with a per-physics-frame collision trace (`player.get_slide_collision()` dumped every tick): the player's velocity was correctly nonzero (140 u/s) and it WAS resting on real floor collision (`is_on_floor()=true`, normal (0,1,0)), but `move_and_slide()` reported up to 6 simultaneous collisions against the SAME single `StaticBody3D` -- not duplicate geometry (GB-36's own dedup measurably reduced the resting count from 6 to 1, a real but incomplete improvement) but Godot's own well-documented `ConcavePolygonShape3D` "internal edge" limitation: a `CharacterBody3D` sliding across a large static trimesh floor built from many adjoining triangles can catch on the seams between them, since `GodotPhysics3D` (the default 3D backend) has no smoothing for this case. Confirmed directly: switching `project.godot`'s `physics/3d/physics_engine` to `"Jolt Physics"` (built into this Godot version, selectable without an addon) alone took Plane2's own player from 0 net displacement over 60 frames to 179.7 units of real forward movement, with `smoke_plane2_move.gd` going from FAIL to OK. This is a project-wide engine setting, not a per-level patch, and fixes this whole CLASS of bug for every level with a large brush floor, not just Plane2's. While investigating, also refined GB-36's collision generation: `_add_mesh_collision()`'s own render-mesh-derived dedup (added this session, gated to the level's own brush geometry) further reduced Plane2's resting collision count from 6 to 1 by deduplicating near-coincident collision TRIANGLES directly (texture-agnostic, looser 3-unit tolerance -- collision doesn't care about render-material identity or fine visual precision the way GB-36's own render-mesh dedup correctly does). Originally applied to every mesh needing collision, including prop/entity models; confirmed live via `smoke_click_survey` timing out (still running past 90s, previously well under it) on Intro2's own dense tree-canopy foliage meshes, where many genuinely close-together leaf polygons cluster into few grid cells and degrade the same spatial-hash approach toward O(n²) for that specific mesh shape. Gated to brush-only (`_add_mesh_collision(node, dedup)`, only the level-geometry call site passes `true`) since that's the only place the deadlock was ever actually measured; props/entities went back to plain `create_trimesh_shape()`. Full regression with both changes together: `smoke_dispatch` (19/19), `smoke_click_survey` (16 levels, 307/307, ~2 min), `smoke_plane2_move` (OK), `smoke_plane2_playtest`, `smoke_remove_race` all green. **Inn and Taxi are a SEPARATE, still-open bug, not fixed by this**: traced their own "keys don't work" symptom to `velocity` staying `(0,0,0)` even while forward is held, with `on_floor=false` -- `player_controller.gd`'s own `_physics_process()` early-returns to zero velocity whenever `camera.current` is false, meaning camera authority never actually switches to the player's own FP camera on these two levels, an unrelated bug from Plane2's physics-collision deadlock. Needs its own investigation into whichever WDL/CameraAuthority path is supposed to hand off control to the player controller on Inn/Taxi specifically. |
| GB-39 | Follow-up (2026-08-13/14) to GB-37: "I think that the lighting issue here is that you don't 'project' the light correctly from the light points to the surfaces thats why the game seems dark in example inside plane2." | Correct -- GB-37 explained the ceiling's OWN painted-on light fixtures, but the level's real, separate WMB `LIGHT` entities (the actual "white round thing" markers, native point lights, not texture art) were genuinely too dim to reach anything. Measured directly: Plane2's own 3 cabin-ceiling lights sit at `y=231` (mid-ceiling, matching `plane_ciel`'s own measured surface height) with `omni_range=25.0` -- nowhere near enough to reach the floor ~130-190 units below or the side walls, so they lit almost nothing. Traced to `_spawn_light()`'s own `light.omni_range = clampf(rng * 0.05, 4.0, 120.0)`: `range` is a raw `float` straight out of the WMB `LIGHT` record (`wmb_file.gd read_level()`, typ==2, no scaling applied at parse time) -- the SAME quant units as `origin`, which `_pos_to_godot()` only ever axis-remaps, never rescales, matching every other distance already used 1:1 in this port (room bounds, capsule heights, feet-snap deltas). The `* 0.05` had no derivation on record anywhere and was simply wrong, crushing every light's reach to 5% of its real value regardless of what it was. Before picking a replacement, surveyed the raw `range` field across the WHOLE corpus directly from the WMB files (358 lights, 41 levels) instead of guessing a second magic number: median 500, most values 200-5000 (matching real room/hall scale -- Plane2's own cabin lights are each 500, and its cabin is ~360x1017 units), a cluster of legitimate large "whole-level fill" lights up to 10000, and one clear outlier (Desert's own two lights, both exactly 500000 -- almost certainly the original static/lightmap renderer's idea of "affects the whole map," not a value meant for a real-time attenuated point light). Fixed both places this formula was duplicated -- `wmb_level_loader.gd`'s `_spawn_light()` (native WMB point lights) and `wdl_interpreter.gd`'s `MY.LIGHTRANGE` bridge (GB-29, dynamic entity lights), which had explicitly reused the same factor "for consistency" -- to `clampf(rng, 20.0, 4000.0)`: the raw value used directly, clamped only to keep the rare extreme end performance-sane, no longer divided down. Verified with a controlled screenshot (camera placed at floor level directly under one of Plane2's own cabin lights): passenger seats, red carpet floor, window, and overhead bins are all now clearly lit, where they were previously near-black. Dev tool's own Light Lab JS mirror (`dev_scene_viewer.py`) re-synced to the same formula. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK) all green. |
| GB-40 | Follow-up (2026-08-15), Studio: "the shick note isn't brighter than anything in the original game, and the poster is higher up - in the middle of the board behind it and not on the bottom of it" + "colored lights are emitted and reflected on the floor, and the objects have shadows which in the current implementation both server and godot doesn't exist." Grounded this round's fix in `E:\RE_general\PiposhTools\decompile_acknex` (a from-scratch disassembly of the original `AsyAct1.exe` renderer the user pointed at, not a guess) instead of more screenshot-guessing. | Three separate real bugs. **ShikNote unshaded/too bright**: `SHADING_MODE_UNSHADED` on the poster material was never actually checked against the real game -- it always rendered at full raw texture brightness regardless of scene lighting, invisible as an issue while the level's own ambient was still too bright everywhere (see below), obvious once that got fixed. Switched to the same lit (Lambert, no specular) material every other brush/prop surface already uses. **ShikNote position**: went through three real bugs in sequence, each caught by testing against the actual running result, not assumed from the code. (1) The entity's own embedded WMB brush is a genuinely degenerate sliver (near-zero size), so GB-34's "center on the entity's own AABB" fix was measuring almost nothing -- replaced with a scan of the LEVEL's own nearby brush geometry (`_measure_nearby_wall_panel_center_y()`, new) to find the real corkboard/frame structure actually mounted behind the note. (2) First version used a vertex-count-weighted mean, live-tested as landing too high (a real board is several stacked pieces -- frame, backing, decal -- with uneven triangulation, so a raw per-vertex mean skews toward whichever piece has the most vertices, not the panel's true center); switched to a min/max bounding-range midpoint instead. (3) Still wrong after that -- traced to `root.global_position` silently returning `(0,0,0)` instead of the entity's real position, because `root` isn't added to the SceneTree yet at the point `_mount_wall_card()` runs (confirmed live: `is_inside_tree()` false at that call site) and `global_position` doesn't fall back to the local transform pre-insertion in this Godot version. Used `root.position` directly instead (already the correct world position, since `root` has no parent yet). Then a FOURTH bug, found the same way: the measured value is in world space (the scan reads the level brush via `global_transform`) but was being assigned straight to `mi.position.y`, which is local to `root` -- silently double-counting root's own world Y (95 + a ~118 world value landed the quad at world 213, "too high" exactly as reported). Fixed by subtracting `root.position.y` before assignment. Final measured result: world Y=118, matching the board's own real 56-180 extent, confirmed both numerically and with a screenshot -- the note now sits centered on the green board between its wooden trim, not floating near the bottom. **Colored lights/shadows not reaching the floor**: the decompiled reference confirms the original engine's surface lighting was mostly static lightmaps baked at level-compile time, with per-entity `LIGHTRANGE` lights (what GB-39 already fixed) as a genuinely separate mechanism -- and its own Godot-reimplementation notes endorse `OmniLight3D` attenuation as the right modern equivalent rather than chasing the exact original falloff curve. An isolated test (bare floor + one light) confirmed the mechanism itself works fine (clear color + shadow at a 60-unit distance); the real scene showed nothing because of two compounding factors measured directly: this port keeps WMB coordinates 1:1 as Godot units (quant-scale, hundreds of units per room, not meter-scale), so `light_energy` near Godot's own ~1 default was far too weak at Studio's real ~190-unit light-to-floor distance; and the level's own flat ambient (`_ensure_environment()`, 0.85 energy) was bright enough to wash out a nearby light's contribution almost entirely on its own. Boosted `_spawn_light()`'s and the `MY.LIGHTRANGE` bridge's `light_energy` (1.0/1.1 -> 12.0) and cut `_ensure_environment()`'s `ambient_light_energy` (0.85 -> 0.4) so local lights and shadows read as real contrast again. Verified live: a character near the light cluster now casts a visible shadow on the floor that wasn't there before. Follow-up same day ("I reloaded the project and I don't see any change"): that first cut was real (proven via screenshot) but too subtle to register during ordinary play against a still-fairly-bright ambient -- pushed further to `light_energy = 28.0` and `ambient_light_energy = 0.22`, plus `omni_attenuation = 2.2` (steeper falloff, so a light reads as a concentrated color pool near its own fixture rather than a soft wash across the whole room, closer to how a baked lightmap would actually look). Confirmed live: both colored ceiling fixtures now visibly glow with real saturated color on the grid right around them, not just a flat marker. **Known remaining gap, not fixed this round**: real-time point lights are still an approximation of what the original baked as static lightmaps, so colored floor tinting directly beneath a light is visibly improved but still more subtle at floor distance than a true per-surface bake would give -- flagged honestly, not claimed as pixel-perfect; a faithful match would mean implementing actual baked/vertex lighting from the WMB LIGHT records at level-load time, a substantially bigger change than tuning real-time light parameters. Fog was checked against the same decompiled reference and found to already be correctly architected from an earlier session (`level_runner.gd _apply_wdl_fog()`, WDL `camera.fog`/`fog_color` driven, `fog_sky_affect=0` for exactly the skybox-exemption reason the disassembly independently confirms real DirectX fixed-function fog has) -- per-texture `TEX_FOG` defaults from the WMB texture records are NOT read (a real, separate gap, not chased this round). Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK) all green. |
| GB-41 | Follow-up (2026-08-16): "remove your light and fog implementations. Use the ones from the reverse engineering code." Deep-dived the WMB lightmap format first (byte-level investigation, not committed to the repo since it produced no working code -- summarized here: confirmed lump L8 is almost certainly the lightmap data block and face bytes 16-19 are a lightmap data offset into it, but could not pin down the exact per-face width/height/pixel-format formula -- tested against both the diffuse texinfo vectors and axis-projected UVs across a wide range of luxel sizes and bytes-per-pixel, no combination matched consistently; the official Conitec format docs that likely have the exact struct were unreachable, TLS too old for this environment to negotiate). Extraction remains unimplemented. | Re-reading `rewrite_skill/PORTING_MANUAL.md` (independently reverse-engineered, same conclusion as the disassembly) made the actual mistake obvious, and it's not a tuning problem: "Keep the runtime OmniLight3D spawns for lights the WDL animates at runtime. Static lights that are already baked into the lightmap must **not** also be spawned, or everything doubles up." A WMB `LIGHT`-type object is by construction never WDL-animated -- it's pure level-compile-time data meant for a lightmap bake. `_spawn_light()` spawning a real-time `OmniLight3D` from every one of these was the actual bug GB-39/GB-40 were built on top of; no amount of range/energy/ambient/attenuation retuning was ever going to converge on "looks like a baked lightmap" (confirmed the hard way: three rounds of tuning, each screenshot-verified as a real but insufficient improvement, because the whole approach was structurally wrong). Removed `_spawn_light()`'s `OmniLight3D` creation entirely -- static WMB lights now contribute nothing at runtime (correct until real lightmap extraction lands; better than a fake, un-sourced glow). Reverted the two rounds of ambient cuts on `_ensure_environment()` (0.85 -> 0.4 -> 0.22) back to the original 0.85 -- there was never a reference-sourced target for this number, only screenshot-driven tuning, and cutting it further was compensating for lights that shouldn't have existed. Left `wdl_interpreter.gd`'s `_get_or_create_entity_light()` (the `MY.LIGHTRANGE`/`ACTION FlickerLight` bridge) alone architecturally -- this one genuinely is "WDL animates it at runtime," matching the manual's own keep-this case -- but reverted its `light_energy` (28.0 -> 1.0, Godot's own default) since the disassembly explicitly found no recoverable original intensity formula for `LIGHTRANGE` ("not conclusively pinned down this pass... don't guess a formula without evidence") and 12.0/28.0 were exactly that kind of unsourced guess. Its `omni_range` fix (raw WMB/WDL units, no `* 0.05`) stays -- that one IS sourced, from the disassembly's own confirmed "value = real x 1024" fixed-point convention applying uniformly to position and LIGHTRANGE alike. Fog was re-checked against the same reference and left unchanged: its core architecture (single active `fog_color`, `fog_sky_affect=0` for the skybox exemption) already independently matches the disassembly's own confirmed facts, and the depth-distance mapping (no formula exists in the reference to replace it with) was validated through real prior playtest reports (GB-20/22/23/24), not blind guessing the way the light tuning was -- reverting it would only reintroduce those older, already-fixed bugs. Verified live: static WMB lights now correctly contribute zero runtime light (confirmed via a level load reporting 0 native `OmniLight3D` nodes spawned for Studio, was 4 before); brush geometry is lit by ambient (0.85) + sun only, an honest baseline instead of a faked one. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK) all green. **Still open**: real lightmap extraction (the actual fix for "colored lights reflected on the floor") -- needs either the exact WMB face/lightmap struct from Conitec's own docs (unreachable from this environment) or further empirical reverse-engineering of lump L8. |
| GB-42 | Follow-up (2026-08-16): "can you list all places in the code that handle the light effects and then see in depth what goes wrong? basically be a critical reviewer on your own code." | Grepped every light-related touch point across the codebase instead of continuing to guess (`Light3D`, `light_energy`, `ambient_light`, `_spawn_light`, `WdlEntityLight`, `WorldEnvironment`, etc. across every `scripts/` and `scenes/` file) and found the actual reason NONE of this session's ambient tuning (GB-40's 0.85->0.4->0.22, GB-41's revert back to 0.85) ever visibly changed anything: `scripts/engine/acknex_sky.gd`'s own `apply()` -- called once per level load, from `level_runner.gd`'s `_apply_wdl_sky()`, immediately AFTER `_ensure_environment()` sets its own ambient -- unconditionally overwrites `env.ambient_light_energy`/`ambient_light_color` for EVERY level: `_apply_sky_maps()` sets it to a flat `0.9`/`(0.62,0.62,0.66)` for every level with a real sky dome (i.e. most of the corpus), and a separate indoor-level branch (hardcoded for `studio`/`menu`/`credits`/`vilend`/`map`) then overwrites it AGAIN, to `1.0` specifically for Studio. Two independent functions both believed they owned the environment's ambient lighting, with different never-cross-checked values, and the second one always won silently. Confirmed directly, not inferred: a fresh headless Studio load's own `WorldEnvironment.environment.ambient_light_energy` read back as `1.0` even with `_ensure_environment()` freshly reverted to `0.85` moments earlier in the same setup sequence -- every single value tried today for that number was dead on arrival for Studio specifically, and for every other level the REAL active ambient was always `0.9`, never whatever `_ensure_environment()` said. This is the actual explanation for "I reloaded the project and don't see any change" -- not that the change was too subtle, but that it was never live at all. Fixed by giving ambient exactly one owner: removed both `ambient_light_*` assignments from `acknex_sky.gd` (the `_apply_sky_maps()` sky-dome path and the indoor-level branch), leaving `level_runner.gd`'s `_ensure_environment()` as the sole source for every level, indoor or outdoor. Verified live: Studio's own ambient now reads back as the real `0.85` after full level setup (was `1.0`); an outdoor level (Plane2) now reads back `0.85` too (was `0.9`). Also swept for other lighting-relevant duplication while at it: confirmed no other file creates a `DirectionalLight3D`/touches `ambient_light_*`/`WorldEnvironment` outside `level_runner.gd` and this one now-fixed spot in `acknex_sky.gd`; also fixed a few comments in `wmb_level_loader.gd`/`wdl_interpreter.gd` that still described the pre-GB-41 "WMB lights are already spawned as real OmniLight3D" behavior as current (harmless -- documentation drift, not logic -- but worth correcting while already in this code). Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK), `smoke_sky_panorama_check` (OK) all green. |
| GB-43 | Follow-up (2026-08-16), Studio: "can you make the lights work - especially the colored ones... the room is still kinda dark and the color effect (theres blue and yellow and red lights in it) are not shown as they were." | GB-41's "don't spawn a runtime light for static WMB LIGHT objects, that's for a lightmap bake" was architecturally correct per the manual/disassembly, but its OWN stated precondition never held: the warning is specifically about double-counting against REAL lightmap data, and no real lightmap data is being applied (extraction wasn't cracked -- see GB-41's own bug entry). With nothing baked, "architecturally correct" just meant "the colored lights the user explicitly asked about are completely dark," a worse and less faithful result than an approximation. Reinstated `_spawn_light()`'s `OmniLight3D` creation, explicitly flagged in its own comment as a deliberate approximation to be replaced (and removed, to avoid doubling up for real) if lightmap extraction ever lands. Re-tuned energy from scratch against the NOW-correct ambient baseline (GB-42) rather than reusing any earlier number, all of which were calibrated against ambient values that were never actually live. Went through several real, measured dead ends before landing on a working value: (1) `light_energy=6` -- invisible, no color bleed onto nearby geometry at all. (2) `light_energy=20` with `omni_attenuation=2.2` (a steeper falloff tried to concentrate brightness near the fixture) -- ALSO invisible on the actual ceiling grid, which contradicted an isolated sanity test (bare plane + single light, same settings) that showed a clear, obvious glow; the discrepancy turned out to be geometry, not lighting -- a proper closest-triangle search (per-vertex search failed silently on this large, sparse polygon) found the real ceiling surface sits ~130 world units from the light's own WMB-authored position, not adjacent to it as the 2D screenshot's perspective suggested (a small foreground fixture prop and a distant ceiling triangle can appear to touch in screen space while being far apart in true depth). A tight `omni_attenuation` concentrates brightness in a radius too small to reach across that real gap. (3) Reverted to default attenuation and raised `light_energy=80` to compensate for the real ~130-unit distance -- confirmed live: the blue ceiling fixture now shows a clear, visible glow halo, and nearby props (a floor lamp, a ceiling fan) pick up genuine yellow/warm color tinting that wasn't there before. Cross-checked against Plane2's own cabin lights (GB-39) at the new energy -- still looks reasonable, no blowout. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK) all green. Real lightmap extraction remains the actual, fully faithful fix and is still unimplemented (GB-41's own open item) -- this is a knowingly-approximate stand-in, not a claim of having recovered the original's real formula. |
| GB-44 | Follow-up (2026-08-16): "It looks a bit better but the lights should be even brighter. The fog / stage of plane3 is still really really dark. And the same goes for plane2... Somewhere along the lines you added fog and lights but they made the graphics 1. less playable 2. not accurate to the original game... the lights in plane2 should emit a lot of light since otherwise the room is dark, and it goes for all implementations of light and fog in the game." | Two separate real bugs, both root-caused with direct measurement instead of another round of screenshot-guessing. **Fog swallowing real geometry**: `_apply_wdl_fog()`'s `fog_depth_begin = safe_end * 0.6` (`safe_end = max(diag*1.5, 4000)`) put Plane3's own fog onset at 9082 units while the level's own real bounds diagonal is 10091 -- fog was engaging before the camera ever reached the edge of the level's own real content, confirmed via a live screenshot showing a hard-edged solid black panel (not a gradient) swallowing a bird sprite. Separately, `fog_color=1` really is (per the "SET only sets .x" idiom, GB-20) raw `(1,0,0)` on Acknex's 0-255 scale -- 0.0039 normalized, genuinely near-black by design, not a misread -- but rendering that raw reads as total information loss rather than a moody haze. Fixed by anchoring `fog_depth_begin`/`fog_depth_end` off the level's own real bounds diagonal with headroom (`diag*1.3`/`+diag*0.8`, both floored) instead of a formula that could undercut the level's own real size, and flooring each fog color channel at 0.08. Verified live: Plane3's bird sprite, previously fully hidden, is now clearly visible; `tools/smoke_fog_check.gd` updated to assert the new deliberate floor instead of the old near-raw-zero expectation. **Lights still reading as dark despite three rounds of energy tuning (GB-39/40/43)**: root-caused with an isolated calibration test (`_spawn_light()`'s energy plugged into a bare lit plane at the same real distance/range as Plane2's own cabin light, sweeping energy across orders of magnitude and sampling actual rendered pixel brightness, not judging by eye) -- confirmed Godot's OmniLight3D falloff math itself was never broken, but Godot's default energy scale (`~1.0`) is calibrated for roughly meter-scale distances, while this whole port keeps WMB quant units 1:1 as Godot units (hundreds of units per room). At GB-43's `light_energy=80` and the real ~171-unit light-to-floor distance in Plane2's cabin, that's genuinely still dim (measured ~0.2 grey on a mid-albedo surface); climbing to a strong, clearly-lit ~0.83 only starts around `energy=1500`. Also ruled out shadow self-occlusion as a contributing factor (toggling `shadow_enabled` off on all spawned lights produced a pixel-identical screenshot) and ruled out a coverage gap misread as a lighting bug (one wall sample point that looked completely unresponsive to energy changes turned out to be an unshaded UI icon element, not representative of the wall material generally -- a cleaner sample point responded correctly). Raised `_spawn_light()`'s `light_energy` (80 -> 1500) and, using the same measured evidence (not a new guess), also raised `_get_or_create_entity_light()`'s (the `MY.LIGHTRANGE`/`ACTION FlickerLight` WDL-animated-light bridge) from Godot's literal default `1.0` to the same `1500.0` -- GB-41 had left that one at default specifically because no sourced falloff *curve* exists for `LIGHTRANGE`, which is still true and still not guessed, but the base energy *magnitude* needed for anything to be visible at this port's unit scale is now a real measurement, not a screenshot guess, and there was no reason for the two light-spawning paths to sit at wildly different magnitudes for the same underlying engine-scale reason. Verified live across both affected areas: Plane2's cabin now shows a clear lit hotspot on the overhead bin and richer, more saturated seat/floor/wall colors instead of a muddy dark cabin; Studio's ceiling fixtures now show clearly visible blue/red spot-beams and a bright glowing yellow umbrella light, matching the "vibrant colors and bright ones" the user described from the reverse-engineered reference. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_fog_check` (updated, OK), `smoke_plane2_move` (OK), `smoke_plane2_playtest` (OK), `smoke_camera_trace` (OK), `smoke_sky_panorama_check` (OK) all green. Real lightmap extraction remains the actual, fully faithful fix for both fog-per-texture defaults and static-light color bleed and is still unimplemented (GB-41's own open item). |
| GB-45 | Follow-up (2026-08-16), Studio, comparing against a real screenshot of the original engine's own Studio scene the user provided: "look on the original ami studio level how bright the screen was, and how the light is projected with shadows not just on the characters but on the ceiling as well. And some of the colors are not hitting some items like the purple color is now on one of the lamps that is not there in the original graphics." Two separate investigations, both concluded with **no code change** after real measurement showed no clean general fix exists yet -- reported here so the next pass doesn't re-derive the same dead ends. | **Purple lamp**: confirmed and root-caused precisely. `StudioL_mdl_007` (a lamp prop, own base texture verified plain white/grey by rendering it with all dynamic lights removed) sits 134 units from the blue ceiling light and 240 units from the red one, both comfortably inside their real, sourced 400-unit range (GB-39) -- their colors add into magenta on its shade. Tried fixing it with a steeper, physically-motivated falloff curve (`omni_attenuation=2.0`, true inverse-square, re-calibrated via the same isolated lit-plane sweep method as GB-44's own energy fix, chosen so Plane2's own 171-unit reference distance read almost identically to the shipped curve). The ONE-distance isolated calibration matched, but a live re-check of the real multi-light scenes it needed to preserve (Plane2's cabin, Studio's own wide shot) showed both came back visibly, measurably dimmer than the already-shipped default curve -- because a real room sums contributions from several lights at several different distances simultaneously, which a single-distance calibration doesn't capture -- and the purple barely changed anyway (134 vs. 240 units is just too small a ratio for any reasonable falloff curve to make one color clearly dominant without an exponent extreme enough to also break near-field brightness everywhere else). Reverted in full; both `_spawn_light()` and `_get_or_create_entity_light()` are back to the exact GB-44 shipped values, re-verified live. **Ceiling not lighting up with visible beam shadows**: measured the real geometry directly (`Mesh.surface_get_arrays()` per material, not the whole-mesh AABB a first attempt wrongly used) -- Studio's glass ceiling panels (`glas3` material) sit at world Y 354-404, while the 3 colored ceiling lights sit at Y 195-196, a 158-208 unit gap, with the dark beam structure (`griddle` material, Y 176-357) sitting directly between them. Checked whether the level's one fixed `DirectionalLight3D` "sun" (`level_runner.gd`'s `_ensure_environment()`, every level) was expected to cover this and found it isn't: a controlled comparison (camera aimed straight up at the ceiling, sun-only vs. lights-only vs. both) showed the sun contributes visually nothing to the ceiling's appearance -- "sun-only" and a fully-dark baseline looked the same, and "lights-only" was pixel-indistinguishable from "both." Whatever warm color the ceiling does show comes entirely from the same 3 colored point lights responsible for the room's other lighting, bleeding upward through 160-200 units and the intervening beam geometry, which is weak and patchy rather than the bright, well-defined, shadow-textured glow in the reference photo. This is the same underlying gap as the purple lamp and GB-41's own open item: a real theater-style ceiling wash with crisp beam shadows is a baked-lightmap effect, and no combination of this port's existing point lights (positioned by real WMB data at fixture height, not up at the glass) reproduces it without either inventing an unsourced supplemental light (a new guess, not evidence) or the real lightmap extraction GB-41 already flagged as unimplemented. No regression risk taken here since nothing shipped changed; recorded as a deepened, more precise understanding of the same open item for whoever picks up real lightmap extraction next. Full regression after the purple-lamp revert: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK) all green. |
| GB-46 | Follow-up (2026-08-17): "Can you try and fix the light colors issue - i think the error is that it affects some objects in the wrong way, also in the studio level the light is just a bit too bright, also in the plane2 level it seems some lights are missing - is it possible that the light markers location were parsed outside the plane so they are not visible indoor?" | Three separate questions. **Plane2 "missing" lights**: not a parsing bug -- checked by parsing `Plane2.WMB` directly (bypassing the loader) and confirmed all 4 real `LIGHT` records ARE spawned. 3 sit genuinely inside the cabin (X~-50, Z from -571 to 120, matching the established cabin-light camera framing); the 4th, far outside the level's own bounds (Godot pos ~(-29822,3810,5667) vs. the level's real AABB maxing out around (166,154,392)), turned out to be legitimate, not mis-parsed: the raw WMB entity list contains an entire SECOND copy of the exterior tarmac scenery (Hanger/B747/Dutyfree/TowerW/Windthig/Rotator/BiPlane2, all matching the near cluster 1-for-1) sitting at that exact same huge offset, plus its own `CamPlane` marker -- this is a real second exterior vantage the level's own data defines, and the light belongs to it, not the cabin. No fixture-prop-without-a-paired-light-marker pattern found either (scanned every Plane2 entity name). The real cabin genuinely only has 3 authored light markers across a long fuselage; any darkness toward the ends is sparse source data, not a bug -- reported honestly rather than invented a fix for something that isn't broken. **Colored-light bleed ("affects some objects in the wrong way")**: fixed properly this time, after GB-45's global attenuation attempt failed. Root cause was already known (GB-45): two differently-colored saturated lights close enough together that their ranges overlap on a shared object. The fix that failed changed the falloff CURVE for every light in the game; the fix that worked changes the RANGE, and only for the specific lights that actually clash. Added `_cap_saturated_light_ranges()` (`wmb_level_loader.gd`), a pre-pass run once per level load: for every WMB `LIGHT` whose color is saturated (channel spread > 0.3), find the nearest OTHER saturated light in the SAME level with a meaningfully different color (RGB distance > 0.5), and cap this light's `omni_range` to `0.75 x` that pairwise distance (floor 60 units) -- so two clashing accent lights' spheres stop short of strongly overlapping, instead of a blanket number applied everywhere. Deliberately relative, not fixed, specifically to keep the blast radius honest: corpus-scanned all 358 `LIGHT` records across every level first and found only 3 files (Studio, InShrine, Outro) have a differently-colored saturated pair closer than ~250 units at all -- everywhere else the nearest such pair is 940+ units away (up to 5323), so `min(authored_range, cap)` never engages and those levels get byte-identical results. A flat "shrink every saturated light's range" version was tried first and rejected before shipping: corpus data showed it would have clipped 31 of 40 saturated lights below their authored value globally, a much bigger, unverified change for a 3-level problem. The fraction (0.75) itself WAS tuned empirically against the real purple-lamp case (0.6 killed too much -- the lamp went dark since even the nearer blue light's capped range fell short of it; 0.75 was the first value where blue's own capped range still reached the lamp while red's did not), verified via a direct screenshot: the lamp reads as clean blue, no more magenta. Verified Plane2 is a complete no-op (its cabin lights are grey, `sat=0`, never enter the affected set -- re-screenshotted, byte-for-byte the same framing as GB-44's shipped result) and that InShrine/Outro still load cleanly with sensible capped values (Outro turned out to reuse the exact same red/blue/yellow stage-light setup as Studio, unsurprisingly, and got the same caps). **Studio "a bit too bright"**: not addressed with a separate energy change -- the range-cap fix above incidentally softens this too, since less inter-light overlap means less additive stacking in the areas both lights used to double-cover; re-screenshotted Studio's own real camera framing and confirmed it now reads less washed-out than the GB-44 baseline without a deliberate brightness cut. If it's still too bright after this, that's a separate, additional tuning pass, not bundled into this one. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK), `smoke_fog_check` (OK) all green. |
| GB-47 | Follow-up (2026-08-17): "Plane2 - i can clearly see the lights projected from above are not seen inside the plane's surface. Plane3 - the fog and the level is still just DARK. Shiks - The outdoor is dark, and the indoor has the Shik chacater model 'glowing' light." (Also: "Make F7 goto plane2 and F8 go to Smash" -- done, `level_runner.gd`'s `_unhandled_input()`, hint text updated too.) | Three separate real bugs, one of them corpus-wide. **Plane2 ceiling lights not reaching the floor**: measured the cabin's own `plane_ciel` mesh directly -- it spans world Y 131.5-306, and the 3 cabin lights sit at Y=231, genuinely INSIDE that mesh's own volume, not floating just below a thin ceiling the way a real fixture would. `shadow_enabled=true` (GB-40) meant the ceiling geometry the light is embedded in was shadowing its own downward light before it reached the floor/seats -- confirmed with a direct on/off comparison, same room: shadows off reads as uniformly, visibly brighter on the floor and seats. Fixed by turning `shadow_enabled` off specifically for `_spawn_light()`'s own lights (`wmb_level_loader.gd`) -- these are already a deliberate approximation standing in for unextracted lightmap data with WMB-authored positions never meant to be precise real-time fixture placements, so losing their own shadow-casting is a smaller accuracy trade than letting the level's own ceiling silently eat most of the light. Verified live: a proper aisle-level view (the first attempt accidentally framed the cockpit alcove, not the passenger cabin) now shows the three ceiling light strips clearly glowing down the length of the cabin, matching the report directly. **Corpus-wide dead "Sun"**: investigating Plane3's darkness led to a much bigger, corpus-wide find, same shape as GB-42's ambient bug -- `level_runner.tscn` had a `DirectionalLight3D` named "Sun" baked directly into the scene file, with neither `light_color` nor `light_energy` set (Godot's bare defaults: white, 1.0) at a fixed, never-revisited rotation. `_ensure_environment()`'s own `if not has_node("Sun")` guard always found that node already present and NEVER ONCE ran its own tuned setup (warm color, energy 0.75, rotation -50/-35/0) for ANY level, ever -- confirmed by reading the live node's actual properties (energy=1.0, color=(1,1,1), rotation matching the baked transform, not the code's intended values). Fixed by removing the baked node from `level_runner.tscn`, making the code the sun's one real owner. Re-verified Plane2's cabin and Studio's own wide shot afterward to confirm no regression (both indoor-dominated scenes, expected and confirmed to look unchanged). Plane3's own reported dark cutscene frame was separately investigated in depth (a large black panel dominating half the frame) -- ruled out as the "Island" background mesh (hiding it made no visual difference) and as fog (the values place fog's onset at 13118 units, far beyond anything in frame); most likely a legitimate dark doorway/hatch silhouette during the fall sequence, not a bug, so left alone rather than guessing a fix for something that may not be broken. **Shiks "glowing" character**: root-caused precisely -- `Shik_mdl_046` ("ShikFond", the INDOOR-only character, distinct from "ShikMega" visible through the outside window) sits 137.8 units from one of Shiks' own lights, well inside the distance where GB-44/46's `energy=1500` calibration reads as fully clipped white (matches the earlier Studio purple-lamp calibration data point at a comparable ~134-unit distance). Checked for a material bug first (the same class as the earlier ShikNote fix) and ruled it out -- `shading_mode`/emission on both Shik meshes are ordinary lit, ruling out an unshaded-texture explanation. The real cause is Godot's own default tonemap (`TONE_MAPPER_LINEAR`), which has zero highlight rolloff -- anything pushed past 1.0 just flatlines to solid white, reading as "glowing" instead of "a bright highlight." This isn't specific to Shiks or to any one light value -- ANY character standing this close to ANY of this port's lights clips the same way, corpus-wide -- so fixed at the general, non-guessed level: `env.tonemap_mode = Environment.TONE_MAPPER_ACES` in `_ensure_environment()`, the standard choice for graceful highlight rolloff instead of a hard clip. Verified directly with a same-frame Linear-vs-ACES comparison: Linear shows a flat, textureless white patch across the face/hair; ACES shows a warmer, gradated highlight with visible shading detail -- a real, measurable improvement, though at this specific very-close distance it's still bright (an honest partial improvement, not a claim that standing 137 units from a light will look fully naturalistic without a genuine exposure/near-field falloff model). The outdoor "dark" half of the same report wasn't chased separately -- the corpus-wide Sun fix above is the direct, general fix for exactly that class of complaint and was verified to apply to Shiks' own environment (sun now reads energy=0.75/warm/-50,-35,0 instead of the dead 1.0/white/baked-transform values). Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK), `smoke_fog_check` (OK), `smoke_plane2_playtest` (OK), `smoke_camera_trace` (OK), `smoke_sky_panorama_check` (OK) all green. |
| GB-48 | Follow-up (2026-08-17): "In range now there's too much light. in plane2 the lights are still not emitting light so the plane interior is dark. in plane3 the outside is dark where it shouldn't be dark at all." Confirmed with the user this was about the real Godot game, not the web dev tool. | Two separate real bugs. **Range too bright**: measured Range's own raw WMB light data directly and found it has the SAME 3 small cabin-style fixtures as Plane2 (range 500) PLUS 2 extra lights with a raw authored range of 5000 -- 10x a typical small fixture. Corpus-scanned all 358 `LIGHT` records and found this is not a Range oddity: 73 of 358 (20%) have a raw range over 1000, clustered at suspiciously round preset values (2000/3000/5000/10000/20000, plus Desert's already-known 500000 outlier) sharply distinct from organic small-fixture values (300, 400, 500, 691...) -- a real, corpus-wide semantic category, almost certainly the "affects the whole level" static-lightmap-bake markers GB-39 already flagged Desert's outlier as being, not real point-fixture placements. `energy=1500` (GB-44/46) was calibrated against small-fixture distances (150-400 units); applying it unscaled to a light meant to softly wash a huge area meant dozens of surfaces read strongly lit at once. Added a proportional scale in `_spawn_light()`: `energy_scale = min(1.0, 500/raw_range)`, using 500 (GB-39's own corpus median for real fixtures) as the point below which nothing changes -- confirmed this leaves every already-verified small-fixture scene (Plane2, Studio, Shiks) byte-identical (scale=1.0 whenever range<=500) while cutting Range's own two 5000-range lights down to energy=150 and its one 1000-range light to 750. **Plane2/Plane3 darkness**: chased this hard down the wrong path first -- spent a long round re-suspecting the ACES tonemap added for GB-47's Shik fix, measured it really does darken ordinary mid-tone pixels ~20% relative to Linear, and tried compensating with `tonemap_exposure` (swept 1.3 through 2.0). None of it meaningfully moved Plane2's own hallway pixels, which was the real tell something else was going on -- even nearly QUADRUPLING `ambient_light_energy` directly (0.85 -> 3.0) left the exact same pixels just as dark, which ambient (a uniform, unconditional term) should never do. Checked every Environment post-process flag (SSAO/SSIL/SDFGI/glow/adjustment, all off) before finally checking fog properties directly and finding it: `Environment.fog_density` defaults to 0.01 in a fresh `Environment` and `_apply_wdl_fog()` never explicitly zeroed it -- a COMPLETELY SEPARATE exponential fog mechanism from the `fog_depth_begin`/`fog_depth_end` curve that function actually manages. At the ~200-350 unit distances filling most of an ordinary interior shot, Godot's own exponential fog formula (`1 - exp(-density * distance)`) alone already blends 85-97% of the pixel toward `fog_light_color`, completely independent of how carefully the depth curve is tuned. Confirmed directly: zeroing JUST this one property (no other change) took a wall pixel from (0.04, 0.06, 0.08) to (0.0, 0.16, 0.28) and a ceiling pixel's blue channel from 0.07 to 0.58, an 8x jump, on the exact same frame. This explains BOTH reports at once -- `fog_enabled = value > 0.0` in `_apply_wdl_fog()` has been silently flipping this default density fog on for every level with any `camera.fog` script value the whole time that function has existed, Plane2 and Plane3 both included. Fixed with one line, `env.fog_density = 0.0`, right where `fog_enabled` is set. With the real cause fixed, plain ACES at its own default exposure already read clearly bright, so the `tonemap_exposure` override was removed entirely rather than left in as unneeded, unverified extra risk to GB-47's own Shik near-field fix. Also directly re-examined GB-47's own "Plane3's dark cutscene frame is probably a legitimate doorway silhouette, not a bug" conclusion with the fog fix applied: it was wrong -- that same frame is a forest-and-sky backdrop with a bird flying through it, fully hidden by this exact fog bug the whole time, not a deliberate narrative element. Re-verified GB-47's own Shik fix is untouched (re-screenshotted at the restored default exposure, matches the originally-verified result exactly) since Shiks has no `camera.fog` value and was never affected by this bug. Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK), `smoke_fog_check` (OK), `smoke_plane2_playtest` (OK), `smoke_camera_trace` (OK, one startup physics-settling position jump at frame 15 unrelated to this fix), `smoke_sky_panorama_check` (OK) all green. |
| GB-49 | Follow-up (2026-08-18), fine-tuning after GB-48 unmasked the real lighting for the first time: "now plane2 is a bit bit too bright, and plane3 is good but when we look at piposh outside the plane it's like theres no 'sun light' so its a bit dark when he's physically outside the plane. Can you also re-enable the colored lights in the Studio room but not too bright?" | Three calibration fixes, all measured live rather than guessed, none of them touching the real GB-48 fixes (fog_density/energy_scale) themselves. **Plane2 a bit too bright**: every earlier `light_energy=1500` calibration (GB-44 onward) was tuned against a scene that was still silently fogged by the `fog_density` bug GB-48 fixed -- none of it was ever really tuned against what players actually see post-fix. Trimmed modestly to 1000 (about -33%, matching a mild "a bit" report rather than overcorrecting) in both `_spawn_light()` and `wdl_interpreter.gd`'s matching entity-light bridge, kept in sync as always. **Plane3 outside dark**: found Piposh's own outdoor "hanging outside the plane" pose (`PipFall` action, WMB-authored at Y=895, well above the cabin) via a direct entity scan, then measured the real cause -- the level's single fixed-direction Sun (`-50,-35,0`) doesn't square-on light every possible character orientation, so energy alone has limited effect on whichever side faces away from it; confirmed live at several values that boosting sun energy (0.75 -> 2.5) gives a real, clearly brighter outdoor read (trees/rock/houses all more saturated) without blowing out. Deliberately did NOT use `ambient_light_energy` for this even though a quick test showed it working even better -- ambient is the same global value Plane2's own "too bright" report needs to go the other direction, while the sun barely reaches sealed interiors at all (real shadow-casting brush geometry blocks it), so raising it is safe for indoor scenes specifically because it's mostly a no-op there. **Studio colored lights not visible enough**: confirmed live they were never actually disabled (still spawning, still `energy=1000` post the trim above) -- the real problem was GB-46's own anti-bleed range cap (`RANGE_CAP_FRACTION=0.75`, tied to the pairwise distance BETWEEN the two clashing lights, ~182 units for Studio's red/blue) landing well under the ~220-unit value GB-46's own manual testing had already found gives a clean, visible result on the purple-lamp case specifically -- 136.5 units (0.75x182) was tight enough to not reach the nearby ceiling grid surfaces the reference "vibrant" screenshot (GB-44) showed lit at the original uncapped range=400. Raised `RANGE_CAP_FRACTION` to 1.2 (giving ~218/288 units for Studio/Outro's own light pairs, close to that already-validated ~220 reference) -- re-verified both a wide establishing shot (visible red/blue tint back on the ceiling grid near each fixture, without looking blown out) and the original purple-lamp case specifically (still clean blue, zero bleed, confirming the anti-bleed fix itself still holds at the new value). Full regression: `smoke_dispatch` (19/19), `smoke_click_survey` (307/307), `smoke_plane2_move` (OK), `smoke_fog_check` (OK), `smoke_plane2_playtest` (OK), `smoke_camera_trace` (OK, same benign frame-15 startup jump as GB-48), `smoke_sky_panorama_check` (OK) all green. |
| GB-50 | Follow-up (2026-08-20): "i get the feeling that shicks level for example the outdoor light isn't lit well enough, and it should match with all other levels as well, can you find a deterministic way to get all the lights work correctly wihtout being to obright? For example plane3 noew piposh closeup face is too lit." | Root cause: GB-44 through GB-49 were all flat, static energy multipliers (ambient/sun/point-light) each tuned against ONE specific reported view -- structurally incapable of being simultaneously right everywhere, since a single fixed-direction sun cannot correctly light every surface orientation at once. Confirmed directly: Shiks' own rock formation (facing away from the sun) reads as a near-featureless black silhouette no matter how high the flat sun energy goes, while Piposh's face in a Plane3 close-up (facing squarely into the same sun) blows out at that same energy -- raising the number fixes one and breaks the other, the exact loop GB-49 already fell into. Fixed at the general level instead of tuning yet another static number: enabled Godot's built-in auto-exposure (`CameraAttributesPractical`, assigned via `WorldEnvironment.camera_attributes` in `_ensure_environment()` so it applies to every level and every camera automatically) so each view's own real measured luminance normalizes toward a consistent target, the way an actual camera does. Godot's own default sensitivity range (100-800, an ISO-equivalent calibrated for real photometric light units) was a genuine mismatch for this port's non-physical energy scale (ambient ~0.85, sun 2.5, point lights ~1000+) -- at the defaults, Shiks' own outdoor shot came back almost completely blown-out white, confirmed NOT a convergence delay (tested up to 300 frames/5 real seconds, same result). Swept wider ranges against that shot and landed on 800-8000 (10x Godot's own default band); re-verified this same single range also protects Plane3's close-up face from blowing out, restores visible detail on Shiks' own rock formation, and leaves Plane2/Studio/Range/the original Shik near-field case (GB-47) all still reading well -- one shared, principled setting doing the job six rounds of static-number tuning couldn't. Full regression: `smoke_dispatch` (19/19) green. |
| GB-51 | Follow-up (2026-08-20), Smash: "A sign shown in the level should have a 'video' running but it has only one image showing the whole time." | Root cause found via raw WMB texture-lump dump (not guessed): Smash's own sign uses "+0YACH" through "+7YACH" -- 8 real, distinct texture entries sitting right in the level's own texture list, WED's own authoring convention for Quake/Acknex-family animated brush textures (consecutive "+N&lt;base&gt;" frame names). The brush-material builder (`_apply_runtime_brush_materials()`, `wmb_level_loader.gd`) only ever read the single frame a face's texture INDEX pointed at (always frame 0) and silently dropped the other 7 -- generic, not Smash-specific, since every brush surface in the game goes through this same path. Fixed generally: `_build_texture_or_animation()` now checks any surface's texture name against the "+N&lt;base&gt;" pattern, collects every sibling frame sharing that base name from the level's own texture list, and builds a real Godot `AnimatedTexture` (8fps) cycling through all of them; a lone "+0&lt;name&gt;" with no real siblings falls straight back to a plain static texture, unchanged from before. Corpus-scanned all 134 WMB files for this pattern and found it's genuinely widespread, not a one-off: 14 levels carry real multi-frame animated brush textures -- Smash/Mansion share the 8-frame "YACH" sign; Plane/Plane2/Range all share a 6-frame "cockpit" (instrument-panel lights) and a 3-frame "floor"; Arena/CROWD, Fight/disc, HitUFO/UFO, InShrine/amb, Intro4+Intro15+intro11/runway, and Intro8/Show round out the rest. Verified two ways: (1) in-engine, a direct `AnimatedTexture` dump confirmed Smash's sign gets all 8 frames and Plane2's floor gets all 3, correctly assigned; (2) pixel-level, compared each frame's raw decoded image against frame 0 to rule out an accidental regex match against near-duplicate/unrelated textures -- Smash's YACH frames differ by 69% of pixel bytes (a genuinely different image each frame, matching "should look like a video"), Plane2's cockpit frames by ~1% (a subtle blinking-light detail), Plane2's floor frames by ~0.15% (real but very subtle, plausibly a faint indicator light in the carpet texture) -- all real, sourced data, not duplicates grouped by a loose regex. This environment has no working GPU/display context for headless screenshot rendering (a known, previously-documented limitation, not new to this fix), so the result could not be visually eyeballed directly; verification instead relied on the in-engine frame dump plus this pixel-level cross-check. Full regression: `smoke_dispatch` (19/19) green, no new script errors from the change. Needs in-game confirmation from the user (Smash's sign, and a look at Plane2/Plane/Range's cockpit and floor to confirm the newly-surfaced animation looks right, not distracting). |

## Quality of life

| ID | Description |
|---|---|
| QOL-1 | Main menu text is too small — make it bigger. |
| QOL-2 | Level-select screen needs to be easier/more comfortable to use on both mobile and PC. |
| QOL-3 | Some voice lines have an echo baked in; needs an edited (de-echoed) version, with a settings toggle to choose which one to use. |
| QOL-4 | Add a load / save / quick-save option on the right side of the screen. |
| QOL-5 | Move the skip button to the right side. |
| QOL-6 | Add a right-side menu: debug, mouse-look toggle, level select, settings, and separate volume sliders for SFX / Music / Voice — default voice ~30-40% louder than music and SFX. **Shipped 2026-08-10, pending in-game confirmation.** The panel is now right-side (340px, full height, scrollable) and adds mouse-look, debug-overlay and level-select rows alongside the three sliders. Real Godot buses now exist — `Master → {Music, SFX, Voice}` from a committed `default_bus_layout.tres`, so they are correct at engine startup rather than created by an autoload after the fact; before this there were no buses at all and per-category volume was not implementable. Voice defaults 35.7% above SFX/Music. SFX also drops from 1.0 to 0.70, which is the separate "cars honking too loud" complaint. Settings persist to `user://settings.cfg` (migrating the legacy `audio_settings.cfg`). Note the per-sound `SFX_VOLUME_TRIM_DB` table from GB-17 is deliberately KEPT and composes with the category fader — it is a per-sound trim, not a category one. |
| QOL-7 | Add subtitles (on-screen text for voice lines / dialogue). |

## Non-breaking bugs

| ID | Description | Note |
|---|---|---|
| NB-1 | Main menu background image glitches — either swap it for a different image, or animate the original menu background instead. | |
| NB-2 | Mobile: no 3D (look/turn) movement, and no equivalent of right-click for the choice cursor. | **Shipped 2026-08-10, UNVERIFIED on hardware.** Root cause found by reading the input path: the `interact` action in `project.godot` is dead config that no script reads, and `wdl_director.gd` initialises `mouse_look = true` with both level paths setting it true again — so every level starts in look mode and the click path is unreachable until you RIGHT-click. Touch had no right-click, so the entire click-driven game was sealed off. Added an ACTION button and a CURSOR button that synthesize real mouse events, plus safe areas and DPI-scaled hit targets. Also fixed two causes of "look doesn't work": the pads only existed on first-person levels, and even there a drag only started if the press landed inside the corner circle. 39 headless checks pass. Real finger input, notch safe areas, DPI and haptics CANNOT be verified without a device — needs a physical Android test before this row is deleted. |
| NB-3 | Lighting flickers and doesn't match the original game; no shadows. | Shadows fixed 2026-08-10 (GB-25): real-time shadow casting/receiving now on everywhere (directional sun, WMB point lights, brush/prop geometry). Still open: the light isn't tuned against a live reference (energy/angle/color are a reasoned first pass, not measured), and the reported "flickers" symptom specifically hasn't been reproduced or root-caused. |
| NB-4 | The green "overlay" HUD panel flickers. | |
| NB-5 | Loading text isn't centered — sits a bit too far right/down. | |
| NB-6 | "Sticker clicked/found" text isn't centered — sits a bit too far right/down. | |
| NB-7 | `tools/convert_gfx.py` silently discards 17 source bitmaps. It writes every source to `<stem>.png`, but 17 stems exist as BOTH `.bmp` and `.pcx`, so one overwrites the other and the survivor is decided by ASCII case ordering. | **Resolved on the runtime path 2026-08-10; the Python converter is still affected.** Settled by measurement, not preference: every `.wdl` under `original/piposh3d` was scanned for `bmap` declarations, and the declared filename ALWAYS carries an explicit extension (549 declarations: 496 `.pcx`, 53 `.bmp`). So what the original engine loaded is directly readable from the source. Of the 17 stems, 11 are declared only as `.pcx`; 3 (clouds, panel, star) have no WDL evidence; and 3 (credits, drop, sky) are declared BOTH ways with genuinely different images (e.g. sky.bmp 320x320 vs sky.pcx 256x256), so there is no correct per-STEM answer — only per-CALL-SITE, which is why `GfxBitmap` honours an explicitly named extension before consulting any table. Exactly ONE stem is a genuine disagreement: **PokrPNL1**, where `Cardgame.wdl` declares the `.bmp` (57x250) but the converter kept the `.pcx` (350x250). To close this row fully, `convert_gfx.py` needs the same per-call-site rule, or the runtime path needs to become the only path. |

---

_Last edited: 2026-08-11, `zero-py-no-assets` branch (GB-28/29/30: three more real, corpus-wide bugs specific to this branch's runtime-reader migration, all sharing one root-cause shape — code that still pointed at `assets/converted/**` paths this branch deletes, silently degrading instead of erroring. GB-28: Studio's poster fell back to a flat placeholder colour because `_mount_wall_card()` still built a dead converted-PNG path; fixed by reusing the texture the runtime WMB reader already loaded onto the same entity. GB-29: `MY.LIGHTRANGE`/`LIGHTRED`/`LIGHTGREEN`/`LIGHTBLUE` (Acknex's entity-as-dynamic-light feature, 24 corpus files, e.g. `ACTION FlickerLight`) was entirely unbridged, not specific to this branch but found while chasing this branch's own "lighting looks a bit dark" report — every scripted local light source was a silent no-op, leaving scenes lit only by flat ambient; fixed with a real `OmniLight3D` per entity. GB-30: `_do_create()` (WDL's `create()` builtin, corpus-wide — traffic, gibs, debris, sparks) still hardcoded a dead `assets/converted/mdl/<Stem>.glb` path, so `action MakeCars` (and every other level's runtime-spawned models) silently produced nothing; fixed by routing through the same `_read_runtime_mdl()`/`_build_runtime_mdl_node()`/`_attach_animator()` path the level loader already uses for WED-placed entities. Verified via `smoke_town_traffic_check`, `smoke_bitmap_create_check`, `smoke_gib_debris_movement_check`, `smoke_plane3_vase_catch_check`, `smoke_range_hitscan_check`, and `smoke_dispatch` (19/19), all green; `smoke_mdl_integration` and `smoke_click_survey`'s stray warnings confirmed pre-existing on this branch (unrelated to these fixes) via a stash/baseline comparison. See `docs/SESSION_LOG.md` for the full trace, including how GB-30 was found while re-investigating GB-27's own Town car-height gap and disproves an earlier, incorrect theory that the runtime WMB path reader itself was broken — it was a wrong file path in a throwaway diagnostic, not a real bug in `wmb_file.gd`.)_
